# Decision log (ADRs)

One file per decision. **Read this index before proposing a plan** — the most common wrong turn is
re-proposing an option that was already rejected.

## Format

```markdown
# NNNN — <short title>
- **Date:** YYYY-MM-DD
- **Status:** decided | superseded by NNNN | reversed
- **Decision:** one sentence, imperative.
- **Why:** the reason in the user's terms.
- **Rejected:** the options we did NOT take, and why.
- **Consequences:** what this forces us to do / stop doing.
```

Numbering: next free number, zero-padded to 4.

## Index

| # | Decision | Status |
|---|---|---|
| [0001](0001-one-html-render-engine.md) | One HTML render engine everywhere; retire native Canvas | decided |
| [0002](0002-presentation-json-per-invoice.md) | Invoice presentation = one JSON per invoice, asset refs | decided |
| [0003](0003-currency-stored-on-invoice.md) | Store currency on the invoice row; client-locked | decided |
| [0004](0004-share-link-is-html-not-image.md) | Share link renders HTML from snapshot, not an uploaded image | decided |
| [0005](0005-edit-mints-new-share-token.md) | Editing a shared invoice mints a new token and revokes the old | decided |
| [0006](0006-g1-real-invoice-metric.md) | G1 "real invoice" = confirmed share or payment; repeat use as support | decided |
| [0007](0007-first-run-currency-ladder.md) | First currency: IP → device region → unambiguous language → USD | decided |
| [0008](0008-adaptive-layout-roadmap.md) | Adaptive layout in 5 phases, applied directly; RTL ships separately | decided |
| [0009](0009-currency-backfill-measured.md) | 2,717 of 5,004 invoices still carry the wrong currency — the backfill's WHERE clause could not see them | measured |
| [0010](0010-health-centre-one-dashboard.md) | Everything that fails silently gets a check in one place; a new check is a `@Component`, never a new page | accepted |
| [0011](0011-discard-dialog-only-for-documents.md) | Small entity forms lose the discard dialog and keep a draft; invoice/estimate keep it — the six copies are two different interactions | accepted |
| [0012](0012-no-continue-editing-dialog-on-entry.md) | A draft is resumed, not negotiated — no "continue editing?" dialog on entry | accepted |
| [0013](0013-logout-clears-session-and-purges-when-settled.md) | Logout clears the session; the account's data is deleted only once it is settled | accepted |
| [0014](0014-discard-dialog-needs-document-content.md) | A business and a client are not work worth a dialog | accepted |
| [0015](0015-estimate-draft-rules-copied-only-where-safe.md) | Estimate draft rules follow the invoice's — but only four of five | accepted |
| [0016](0016-one-bottom-sheet-standard.md) | One bottom sheet, and it answers when you pull it | accepted, shipped |
| [0017](0017-shared-pdf-android-installs-others-download.md) | On the shared link, Android installs the app for a PDF; everyone else downloads | accepted, built |
| [0018](0018-the-shared-image-is-gone-html-is-the-only-render.md) | The shared invoice image is gone; HTML is the only render | accepted, built |
| [0019](0019-two-tags-money-and-delivery-are-not-one-axis.md) | An invoice card carries two tags — money and delivery are not one axis | superseded by 0020 |
| [0020](0020-the-payment-tag-goes-the-card-says-it-in-words.md) | The payment tag goes; the card says it in words | accepted, built |
| [0021](0021-dynamic-colour-is-offered-never-assumed.md) | Dynamic colour is offered, never assumed; it stops at the document | accepted, built |
| [0022](0022-edge-to-edge-verified-for-targetsdk-36.md) | Edge-to-edge verified for targetSdk 36 — 17 surfaces measured, no code needed |
| [0023](0023-one-dismissal-event-the-method-is-a-parameter.md) | One dismissal, one event; the route (`method`) is a parameter on the sheet's own close id | decided, implemented |
| [0024](0024-a-double-tap-is-stopped-at-the-button-not-counted-later.md) | A double tap is stopped at the button, not counted around later | decided |
| [0025](0025-voice-input-is-offered-only-where-the-device-can-do-it.md) | Voice input is offered only where the device can do it | decided |
| [0026](0026-a-session-is-renewed-before-it-dies-never-revived-after.md) | A session is renewed before it dies, never revived after | decided, implemented |
| [0027](0027-a-first-time-user-is-a-device.md) | A first-time user is a device, and the journey is counted per device | decided, implemented |
| [0028](0028-the-invotick-id-is-part-of-the-account.md) | The Invotick ID is minted with the account, not on demand (reverses 2026-07-09 lazy) | decided, implemented |
| [0029](0029-a-sync-failure-is-an-attempt-the-server-refused.md) | A reported sync failure is an attempt the server refused | decided, in app branch |
| [0030](0030-every-device-that-stopped-has-a-reason.md) | Every device that stopped on the journey has exactly one reason | decided, implemented |
| [0031](0031-the-splash-shows-the-app-while-the-gate-holds.md) | The splash shows the app taking shape while the ad gate holds; gate timing untouched | remedy reversed by 0038 (diagnosis stands) |
| [0032](0032-dismissing-the-save-gate-keeps-the-invoice-as-a-draft.md) | Dismissing the Save gate keeps the invoice as a draft | decided, in app branch |
| [0033](0033-no-banner-under-the-first-open-onboarding-overlay.md) | No banner under the first-open onboarding overlay (owner's call; measure impressions alongside) | decided, in app branch |
| [0034](0034-a-page-costs-what-it-shows.md) | A page costs what it shows, not what the database holds | decided, being applied |
| [0035](0035-a-build-must-not-be-able-to-take-the-database.md) | A build must not be able to take the database | partly done, one number still to measure |
| [0036](0036-a-delete-must-say-what-it-is-deleting.md) | A delete must say what it is deleting (sync contract gap G2) | decided, not yet built |
| [0037](0037-the-ui-layer-owns-the-press.md) | The UI layer owns the press; the coded `trackClick` twin goes | decided, in app branch |
| [0038](0038-the-hold-shows-one-branded-loader-not-a-picture-of-the-screen.md) | The hold shows one branded loader, not a picture of the screen it is heading to (reverses 0031's remedy) | decided, in app branch |
| [0039](0039-language-is-read-from-the-session-not-re-sent-per-event.md) | Language is read from the session, not re-sent per event; the session row is written from any batch | decided, in backend tree |
| [0040](0040-the-system-splash-draws-the-mark-we-are-about-to-draw.md) | The system splash draws the mark we are about to draw; we cannot paint sooner than the platform, only paint the same thing | decided, in app branch |
| [0041](0041-premium-is-a-date-on-the-device-refreshed-daily.md) | An account's premium reaches every device of it: Play's flag, or else the server's confirmed grant, held until the server's `offlineValidUntil` — a day inside the store's refund window (Play 48 h, Apple 14 days), the plan's date after it (the owner's design; a build with no window was rejected). Asked once a day after the splash, at once on a sign-in or device-link; the server checks Google each time, at most once per purchase in 10 min. CANCELLED with time left is premium, Google unreachable is never a no. Rejected: one flag for both, asking in the splash, a new route, no window | **approved by the owner 2026-09-14; built for 1.4.6** (backend and app branches `…every-device`); not deployed |
| [0042](0042-the-server-owns-the-version-and-the-device-merges.md) | The server owns the version and the device merges without asking: six push answers, delete carries and bumps its version, derived money fields are recomputed not merged (extends 0036, which has no row for the case that will actually arrive) | **decided, being built** |
| [0043](0043-an-ad-failure-is-named-in-the-sdks-own-words.md) | An ad failure is named in the SDK's own words, on the event that already exists: `reason` beside the untouched `code` because the load and show tables overlap, plus `error_domain`, one cause level, the missing `ad_request_id`, and `path` because `placement` is constant | decided, in app branch |
| [0044](0044-an-install-belongs-to-a-tag-not-to-a-link.md) | An install belongs to a tag, not to a link: the UTM tab joins the registry to `install_referrer` by exact equality on all three UTM values, a zero prints as `0`, and traffic matching no link of ours is listed uncredited | decided, built |
| [0045](0045-the-share-link-page-reports-its-own-journey.md) | The share-link page reports its own journey: six names into `/v2/analytics/track` as `platform=Web`, the app's own names where the action is the app's, `viewer_platform` not `platform`, the version floor exempted for Web, and no App Store link because there is no App Store app | decided, built, deployed 2026-09-09 |
| [0046](0046-a-sent-link-retires-only-when-its-replacement-is-sent.md) | A sent share link retires only when its replacement is confirmed sent: `create()` never revokes a sent token, `markShared()` does; an unsent token is still replaced at once; snapshots compare as content, with embedded images masked | decided, built |
| [0047](0047-plays-answer-decides-premium-on-a-device.md) | Play's answer decides premium on a device, guest or signed in: ads stop the moment Play reports a purchase, another account's purchase still makes this phone premium, "not premium" needs Play's confirmation before any ad is shown, requests never wait, and the splash waits at most 1.5 s from its start | decided, built (next release 1.4.5) |
| [0048](0048-the-paywall-says-what-google-charges.md) | What the paywall says is what Google charges — price, period and saving from the offer actually bought, never a product's name; a refusal is final only when Google gives it (400), "could not ask" is 503; the server keeps tokens and every restore answer; an admin can defer one customer's renewal | decided, built (next release 1.4.5) |
| [0049](0049-the-app-open-ad-is-asked-for-at-the-first-onstart.md) | The app-open ad is asked for at the first onStart, not when the SDK is ready: the only question is the saved premium flag (false by default), Play answers in parallel, the show still refuses premium and still waits (≤1.5 s from splash start) for Play's "not premium"; cold-start requests move from `path=splash` to `path=preload` | decided, built, verified on the Pixel (next release 1.4.5) |
| [0050](0050-every-sync-failure-carries-its-own-evidence.md) | Every sync failure carries its own evidence, joined by one request id: `X-Request-Id` app → server → response → `sync_failure.last_trace_id` → Loki; `sync_failed` gains ids, codes and versions (never field values); the ingest keeps device, version and record and shares the server's signature; promtail stops labelling `traceId`; the panel shows the evidence and the server's log lines | decided, live (backend + panel; app in 1.4.5) |
| [0051](0051-sync-and-billing-each-have-an-owner-agent.md) | Sync and billing each have a dedicated agent (`.claude/agents/sync.md`, `billing.md`) whose file holds that domain's policy in one place; a decided rule is written there first, memory keeps incidents and dated state, and every line is checked against code | decided |
| [0052](0052-the-business-view-defaults-to-all-and-the-last-choice-is-remembered.md) | With more than one business, every business-scoped screen opens on All Businesses until the user chooses; the latest choice (All or one business) is remembered on every open, on the phone and the web, one choice for all screens; the business a new document uses is remembered separately (reverses the 2026-07-25 "never auto-select All") | decided, to build |
| [0053](0053-a-guests-work-joins-an-existing-account-only-when-the-user-says-so.md) | Sign-up moves a guest's work silently; sign-in to an existing account asks once when there is real work; the move is one server step with proof the phone held that guest (no record-by-record takeover); declined work kept 90 days; businesses never merged by name; the 26 records already moved stay | decided, to build |
| [0054](0054-the-share-card-is-rendered-on-demand-and-cached-at-the-edge.md) | The share link's card is rendered on demand and cached at the edge; the Blob path goes (production never had a token) | decided, to build |
| [0055](0055-a-legacy-invoice-without-a-client-is-closed-for-good.md) | The one legacy invoice without a client (class L) is left as it is and never raised again | decided |
| [0056](0056-an-estimate-date-is-stored-as-a-calendar-date.md) | An estimate's date is stored as a calendar date, not a noon-UTC instant (wrong at UTC+12 and beyond); migration first, then code | LIVE 2026-09-13 23:29 UTC: the migration in batch10 (`cd35e52`), the code and the switch at 93 in batch11 (`751d17b`); the three examples read 2026-08-11 |
| [0057](0057-edits-a-phone-gave-up-during-the-clock-bug-are-sent-again-once.md) | Edits a phone marked TERMINAL during the clock bug are sent again once with the phone's copy, only where no other device changed the record since | decided, to build |
| [0058](0058-when-two-devices-changed-the-same-field-the-later-edit-wins-by-a-corrected-clock.md) | When two devices changed the same field, the later edit wins, not the later arrival; the edit time is HLC-style (corrected to the server's clock, never backwards, after what the phone had seen, never in the future); the version still decides whether an edit may apply; a full HLC is rejected (amends 0042) | decided, to build with the receipt number |
| [0059](0059-a-delete-of-a-record-the-server-does-not-hold-is-not-a-sync-failure.md) | A delete answered NOT_FOUND is filed as `NOT_FOUND_ON_DELETE` (the server holds no row of it; nothing removes a synced row), both sides under one signature; the Device sync card shows it and never counts it; logged at INFO; the device is still told NOT_FOUND; an update answered NOT_FOUND stays a defect; the 1.4.6 app half drops the pair on the server's answer, and before sending only with a sent-marker | decided; server live since 2026-09-12 16:10 UTC (`stage` @ `587b33d`); app half A in 1.4.6 (`3cfc2d75`, merged, not released), B not built |
| [0060](0060-a-pull-never-deletes-a-row-and-a-delete-here-is-the-rows-latest-change.md) | Class P: a pull finds the local row by its id alone (deleted or not, whoever's) and never deletes it — an UPDATE for a row it holds, an ABORT insert for one it does not; a delete made on the phone is the row's latest change (contract L7); no Room schema change (nullable `productId` and a changed foreign key both rejected: they turn a loud failure into silent damage) | decided; built for 1.4.6 in `ProductSyncHandler`, not merged; the other 20 handlers open |
| [0061](0061-a-failed-app-open-show-says-who-refused-and-in-what-words.md) | A failed app-open show carries `reason` + `refused_by` (`guard` = our check in `show()`, `sdk` = the SDK's show-table word) on `app_open_decision`; no new event; the splash's `ad_show_failed` outcome keeps its name | decided, built for 1.4.6 (not merged) |
| [0062](0062-a-request-whose-screen-closed-is-not-a-failed-open.md) | A shared-invoice request whose screen closed is not a failed open: the repository lets the cancellation travel; no `reason=cancelled`; `createShareLink` untouched | decided, built for 1.4.6 (not merged) |
| [0063](0063-leaving-from-the-item-form-is-a-named-stop-on-rung-7.md) | Rung 7 names `left_on_item_form` from the screen on the device's last background (by `event_timestamp`); the same rule for rung 3's invoice screen is the owner's call | decided, built on a backend branch (not pushed) |
| [0064](0064-a-shipped-image-the-build-cannot-produce-reports-itself.md) | A shipped image the build cannot produce reports itself: new coded event `bundled_image_unavailable` (`image`, `stored_ext`, `failure` = `not_in_build` / `read_failed`, `exception_class`), once per image per process; a Health Centre card is owed | decided, built for 1.4.6 (not merged) |
| [0065](0065-a-failed-guest-sign-in-carries-its-calls-evidence.md) | A failed guest sign-in carries its call's evidence on `sync_failed stage=guest_auth` (`request_id`, `http_status`, the thrown `exception_class`); not on `guest_login_failed`, which makes no call; amends 0050's table | decided, built for 1.4.6 (not merged) |
| [0066](0066-leaving-from-the-invoice-screen-with-no-tap-recorded-is-a-named-stop-on-rung-3.md) | Rung 3 names `left_on_invoice_screen` (last background on `create_inv_scr`, no tap, back or exit recorded); the ad/premium dialog's taps are denylisted, so the label says no tap recorded, never touched nothing (0 of 81 sent the dialog's coded events); amends 0063 | built on a backend branch at the lead's direction (not pushed) |
| [0067](0067-an-applied-write-says-its-number-and-a-copy-that-changes-nothing-keeps-it.md) | The receipt number's phase 1b, server only, changing no decision: every applied write answers `data: {version, updatedAt}`; a copy that changes nothing keeps its number (judged by Hibernate's own state, its time still stored because the clock judges later copies by it); a delete may carry `{id, version}` as evidence; the all-zero id is no writer; five counters act on nothing. Rejected: the plan's projection compare, and holding the time back | built on backend branch `feat/sync-phase-1b` (`9cb19f8`), not deployed |
| [0068](0068-a-client-is-deleted-like-every-synced-record-and-a-client-in-use-is-refused.md) | A client is soft-deleted and its DELETE is queued (none of the 5 deleted clients had ever come from a phone); a client that any invoice, estimate or payment points at is refused, all or nothing, with a clear message, until the owner rules (A refuse / B hide / C cascade) | decided, built for 1.4.6; in-use rule open |
| [0069](0069-a-full-pull-sends-a-child-only-with-its-parents.md) | A full pull sends a child only with its parents (a line with its invoice or estimate, a payment link with both ends), and the deleted terms a live invoice names as deleted rows; a 1.4.6 phone asks for a full pull only after a delta's miss and waits 1 h / 6 h / 24 h after one that still fails. Accounts that would trap a new phone: 262 → 8. Rejected: deleted invoices as deleted rows, holding the cursor, a server cascade now | decided; backend `d916a6d`, app `62c06408`; repair (Q1) and root (Q2) open |
| [0070](0070-a-confirmed-grant-stays-confirmed-and-only-a-refusal-the-server-names-is-googles.md) | A grant the server confirmed is never marked unconfirmed again; a register refusal is Google's only when the server names it (`400` + `code: "NOT_VALID"`), everything else is "could not ask" and premium stands on Play's word; billing calls send `X-Device-Id`; the paywall prints no blank price and never launches a plan without an offer. Rejected: reading the message, every 400 as Google's, `code` on the shared `ApiResponse` | decided; app built for 1.4.6 (`9d8f7309`…`a56d8071`); server half being built |
| [0071](0071-a-refusal-a-service-raises-answers-the-status-it-carries.md) | A `CustomValidationError` answers the status it carries (400/401/403/404) with its own message, logged at WARN, instead of `500 An unexpected server error occurred`; no throw site, transaction rule or sync answer moves (0 production 500s from it in 15 days). Rejected: making it a `ResponseStatusException` (the JWT filter reads it as a bad token), changing throw sites, one flat 400/422 | decided; backend `15c0820`, not deployed |
| [0072](0072-the-ip-routes-answer-admins-only.md) | `/v1/ip/**` leaves `security.public-paths` and `IpRecordController` is `@RequireRole(ADMIN)`: anybody could list IP records with locations, delete them and spend the paid lookup quota with no token (0 requests in 15 days; the panel's one caller sends an admin token). Rejected: leaving it public, and only taking it off the list (guests would still get in) | decided; backend `fba532c`, live 2026-09-13 00:26 UTC (batch3) |
| [0073](0073-four-more-public-routes-stop-answering-strangers.md) | Four more public routes stop answering strangers: `push-health` deleted (Firebase ids, no caller), springdoc off except `local` (39 → 36 public paths), `refCode` URL-encoded, the phone backfill ADMIN-only with `limit` ≤ 5,000; `admin-login-as-user` (admin password alone → any user's token) deleted on the owner's yes. Rejected: moving push-health under admin, clamping `limit` | decided; built (`7dd87b4`, `801841c`), not deployed; item 5 waits for the owner |
| [0074](0074-the-phone-name-lookup-is-closed-to-everyone-but-admins.md) | `/v1/lookup/phone` (any number → the name users saved for it: 77,625 names, none of them a user's) leaves the public list and answers ADMIN only; the contact upload itself is unchanged and stays a separate open decision. Rejected: leaving it open, a narrowed version first (the door stays open until an app release), only taking it off the list (guest tokens) | decided by the owner; being built on `fix/phone-lookup-closed` |
| [0075](0075-admin-as-user-is-deleted-support-gets-a-read-only-view.md) | `/v2/auth/admin-login-as-user` (admin password alone → any user's 24 h token, writes allowed, unused) is deleted; support gets a read-only "support view" of the user in the admin panel (their data, invoices rendered as the app does, devices, sync failures, premium, a record of who looked). Rejected: keeping it; a safer "as user" (the admin's phone still becomes a second device on the account) | decided by the owner; deletion rides with 0074; the view is to be planned |
| [0076](0076-server-logs-carry-ids-and-codes-never-a-users-values.md) | Server logs name a record by id, group, operation, version and error code — never the values a user typed (the sync push logged every property: 4,579 lines a day, kept for ever). Rejected: keeping values for debugging, masking them | decided by the owner; being built on `fix/logs-carry-no-values` |
| [0077](0077-logs-are-kept-by-user-type.md) | Logs are kept by the user's type — guests 15 days, registered 30, premium 90, no user 30 — through a 3-value `user_type` label and Loki's compactor retention (there was none; 3.3 GB since July). Rejected: a per-user label (the 643k-streams mistake), keeping everything, one age for all | decided by the owner; being built |
| [0078](0078-what-the-web-writes-reaches-the-phones.md) | Every REST (web) write that changes a synced row sets `last_synced_at` and moves `version` like a sync write, so the next delta pull brings it to every phone; the web's invoice delete takes its lines. Payments untouched (their review). Rejected: leaving web writes to the full pull, a push per web write | decided by the owner; being built on `fix/web-writes-reach-phones` |
| [0079](0079-a-deleted-record-is-kept-90-days-premium-a-year-then-purged.md) | A soft-deleted synced record is purged for good 90 days after its delete (premium: one year), only once 0059 is amended so a purged record is neither a sync failure nor re-created, with a children-first purge job and a Health Centre line. Payments wait for their form's review. Rejected: log-style windows (15/30/90), keeping deleted rows for ever | decided by the owner 2026-09-13; not built |
| [0080](0080-a-record-the-database-refuses-is-refused-alone.md) | A record the database refuses (a product name over 255 characters) is refused alone and the rest of its push is written: a push that cannot commit runs again record by record, the refused record is answered `FAILED UNEXPECTED_ERROR` with `data.field`, at most 10, then the old failure. Found: 3 guest phones stuck since 07-01 / 09-11. Rejected: a savepoint or a transaction per record, record by record always, truncating, a new error type | LIVE 2026-09-13 20:26 UTC (batch9 `1d432e6a`). For long names the owner chose both: names up to 1,000 characters are LIVE since 23:02 UTC (batch10, products and clients), and the app caps at 255 (1.4.6) |
| [0081](0081-every-money-screen-totals-in-the-chips-currency.md) | The dashboard, the client ledger and the Analytics tab total in ONE currency — the chip's (the business's own until the user picks another, remembered per business); other currencies convert at the day's rate and a converted figure carries ≈. Rejected: each screen picking the currency holding the most money (the ledger showed $ under a PKR chip; Analytics added $ as Rs) | decided by the owner 2026-09-14; into 1.4.6 |
| [0082](0082-a-clients-ledger-line-keeps-the-clients-own-currency.md) | A client's line in the ledger shows the client's own currency, as invoiced (a PKR client Rs62,500 beside a USD client $640,000); only the overview total converts, with ≈; a client whose invoices mix currencies (legacy) converts with ≈. Rejected: every line in the chip's currency (Crescent Traders showed $225.53 for Rs62,500, moving with the rate) | decided by the owner 2026-09-14; into 1.4.6 with 0081 |
| [0083](0083-ios-sends-its-analytics-through-the-one-shared-drain.md) | iOS empties its analytics queue through the same `AnalyticsFlusher` Android runs (moved to commonMain). iOS decides only when it runs (launch, 2 s after events, 15 min on screen, foreground, background task, background sync), and every iOS event carries a session. Found: no iPhone had ever sent an event (0 rows, ever). Rejected: an iOS-only uploader, sending only from Apple's background scheduler, rewriting queued events, guessing the platform from build numbers | built 2026-09-14 (`63f7174b`, `24134092`), merged into `VC_102_VN_146`; reaches iPhones with build 17 |
| [0084](0084-revenue-counts-real-invoices-only-overdue-from-the-due-date.md) | Every money screen uses the invoice list's rule: drafts and cancelled invoices never count toward revenue, outstanding or overdue, and overdue comes from the due date (a draft is never overdue). Covers the app's Analytics tab, the dashboard's breakdown sheet and the web dashboard. Found: Analytics Revenue differed from the dashboard's Total Revenue, and Overdue read a status nothing sets. Rejected: a rule per screen; the app only | decided by the owner 2026-09-14; web LIVE (`acc666c`); the Invoices count counts real invoices only; app → 1.4.6 |
| [0085](0085-one-lifecycle-for-both-platforms-an-update-is-not-a-first-open.md) | Android's lifecycle observer moves to shared code, and iOS runs it from UIApplication notifications, so iPhones send `app_cold_start`, foreground/background and heartbeat as Android does. An update is never a first open: with no stored count but an analytics database already on disk, the count starts at 2. Rejected: an iOS copy, counting from 1, the device id or the main database as evidence, iOS network stamps now | built 2026-09-14 (`95558880`, `0234aa09`), merged into `VC_102_VN_146`; iPhones get it in the build after 17 |
| [0086](0086-the-server-remembers-each-phones-last-pull.md) | The server keeps each phone's last pull and starts the next delta pull from there, whatever bookmark the phone sends. So builds already on phones stop skipping changes and deletes made on the web or another phone. A small migration ships first and alone. Rejected: only the app half (phones that never update keep skipping); always sending one day back | decided by the owner 2026-09-14; built after the receipt number |
| [0087](0087-ci-keeps-running-on-the-production-box.md) | CI's test, docker and deploy jobs keep running on the production VPS. Found: on 2026-09-13 a test job raised the load from 0.59 to 28.4 and 25 requests failed; 1,870 CI minutes in 30 days against GitLab's free 400. Rejected: a separate small CI server (~$5–10 a month), GitLab Premium ($29 per user per month) | decided by the owner 2026-09-14; revisit at the VPS move |
| [0088](0088-every-analytics-event-carries-its-platform.md) | `analytics_events` gains a `platform` column (Android/iOS/Web), filled by the server on arrival. Old rows are filled only with what can be proven (before the first iOS row: Android unless a web row; after it: the session's platform), and NULL where unknown. The migration goes first and alone, then the code. Rejected: waiting for the App Store launch (the recommendation), guessing from build numbers, reading the platform through sessions for ever | decided by the owner 2026-09-14; LIVE: the column 00:21 UTC (batch13), the code 00:47 UTC (batch14), backfilled 01:44–01:57 UTC (416 stay NULL) |
| [0089](0089-uxcam-leaves-the-app.md) | UXCam (session replay, gated by Remote Config `UX_Cam`, not known to be on, recordings not watched) leaves the app, together with the two unused image-crop libraries that bring test libraries and three exported test screens into release. Rejected: keeping it and checking first | decided by the owner 2026-09-14; built (`d4fb2ac7`) for 1.4.6; the `UX_Cam` key must stay empty for older builds |
| [0090](0090-the-qr-scanner-stays.md) | Device Link's QR scanner keeps Google ML Kit (323 KB, 12 opens in 30 days). Rejected: Google Code Scanner, which saves 0.32 MB but stops working on phones without Play Services and needs new testing | decided by the owner 2026-09-14 |
| [0091](0091-support-sees-a-users-app-read-only.md) | Support sees a user's app read-only: a Support tab on the user page, backed by seven admin-only reads. Emails and phones are masked on every list; names and addresses are not. A reveal needs no reason. Every look is recorded as a log line, a counter and a `support_view_log` row. Found: `/v1/ip/**` had returned full user records with no login. Rejected: continuing the old single-user pages; a search in the query string | LIVE: the table 23:54 UTC (batch12), the code 00:47 UTC (batch14 `ad625178`), the panel 00:53 UTC (`c2e414b`) |
| [0092](0092-no-password-reset-after-the-ip-route-exposure.md) | After the /v1/ip exposure (BCrypt hashes of 516 real accounts reachable with no login, 2026-04-22 → 09-13; no evidence of access in the logs that exist), no password reset and no new step for users. Rejected: an email code at the next password login (the recommendation), logging everyone out | decided by the owner 2026-09-14 ("Kuch nahi"); users not told, the record kept ("Nahi, sirf record"); tell them at once on any evidence of access |
| [0093](0093-invoices-proven-a-day-early-are-repaired-on-the-server.md) | Group A: the 1,745 invoices proven a day early (the phones' share copies show 61 of 63 exactly a day ahead; builds up to 1.4.0) move one day forward on the server, before 1.4.6, so the 1.4.6 full pull carries the right day to every phone. A dry run first, then the owner's go; an undo register (a migration, alone); a 120-day echo guard. Rejected: a repair driven by the phones (after an update they hold the wrong day), one rule for every invoice | decided by the owner 2026-09-14; **repaired 2026-09-14 09:20 UTC: 1,745 moved, 258 owners, 0 failures, run `84e1a19e`** (undo register batch15, code batch16) |
| [0094](0094-the-phone-number-name-lookup-is-removed.md) | `/v1/lookup/phone` returned a name for any phone number, taken from 509 users' uploaded address books (81,923 names). It answered with no login until 2026-09-13. It is removed from the server, and the app's Contacts "Verify" button is removed in 1.4.6. The stored names and the upload wait for the contacts-upload decision. Rejected: keeping it admin-only; narrowing it to "is this number on Invotick" (the owner's receiver requirement is built on a verified user's own identity instead) | decided by the owner 2026-09-14 ("Poora hata do"); being built |
| [0095](0095-the-app-stops-sending-the-address-book.md) | 1.4.6 stops sending the phone's whole address book, which went out when a user imported one client (530 accounts, 393,974 contacts, about 740 MB; 20–50 a day since 2026-09-03). "Import from contacts" keeps working on the phone. The server's `/v1/contacts/ingest` stays open for now and closes later. Rejected: closing the server route now too (the recommendation), keeping the upload behind a consent screen, leaving it as it is | decided by the owner 2026-09-14 ("pehly app… baad main server"); being built for 1.4.6 |
| [0096](0096-the-stored-contacts-are-kept-for-a-marketing-plan.md) | The contacts already on the server (393,974, about 740 MB) are kept, for a marketing plan the owner will make. If it works, the upload returns in a form that complies with policy. The doc records what the plan must start from: the people never agreed; Play limits use to what the user was told; PECA 2016 and foreign consent rules apply; a lawyer's review is needed before any message. Rejected: delete now (the recommendation), delete when the route closes | decided by the owner 2026-09-14 ("is ko rakho") |
| [0097](0097-the-receiver-tag-is-built-after-146-in-steps.md) | The owner's receiver requirement is built after 1.4.6, in steps: the wording and settings, then verified email, the yes/no tag in the sender's client list, then a notification and inbox that open the existing approve/reject view, then iPhone push once APNs exists. Today about 60 of 6,306 clients could match. Rejected: building it into 1.4.6, not building it for now | decided by the owner 2026-09-14. The same day: wording "On Invotick"; identity comes automatically from the user's own actions (nobody is asked to verify); the receiver gets a "Show that I'm on Invotick" setting, on by default; the goal behind it is automatic payment-due messages (in the app and by WhatsApp); the receiver hears only when the sender shares or sends a reminder ("Sirf jab sender bheje") |
| [0098](0098-a-session-the-app-ended-reopens-with-the-email-filled.md) | A1: after the 2026-09-03 key change (the old key had leaked), about 17 registered accounts still meet a sign-in screen. No server change; the old key never returns. In 1.4.6, when the app ended the session itself, the existing sign-in screen opens in a "sign in again" mode: a line saying the invoices are safe, the email filled in (kept through an involuntary sign-out only), the password one tap away from Google's or Apple's password manager, and no "Login as Guest". Rejected: accepting the old key for a while, a silent re-issue, keeping the password on the phone, wording only | decided by the owner 2026-09-14; being built for 1.4.6 |
| [0099](0099-a-phone-cannot-remove-itself.md) | A guest phone removed itself from its own Linked devices screen (2026-09-13), because the app never told the server which phone was asking. Its syncs have been refused ever since, and its work is stranded. The server now refuses a revoke that doesn't name the calling phone (builds up to 1.4.6 can't remove devices until they update); a removed phone gets 403 and no new pass; every refusal names its phone and build. That guest's one row is re-admitted after the deploy. In 1.4.6 the app names itself, and a removed phone stops, keeps its data and says so. Rejected: the guard only, nothing | decided by the owner 2026-09-14 ("Dono karo"); batch17, then the re-admit, then 1.4.6. Same day: a correct password sign-in re-admits a removed phone ("Haan, password se wapas") |
| [0100](0100-phones-delete-their-cached-address-book-once.md) | Up to 1.4.5, "Import from contacts" copied the whole address book into the app's own database before uploading it. The picker never used that copy, since it reads the phone fresh each time. The copy stays on phones, survives sign-out, goes into phone backups, and outlives the user's permission. 1.4.6 deletes it once, at the first open after the update. Contacts, saved clients and the picker are untouched. Rejected: leaving it | decided by the owner 2026-09-14 ("Haan, aik dafa mita do"); being built for 1.4.6 |
| [0101](0101-a-renewal-key-renews-a-pass-without-signing-in.md) | Each signed-in phone holds a random renewal key, which the server keeps only as a hash, so a signing-key change or a 90-day absence no longer forces a sign-in. The phone exchanges it once for a new pass and key; a replayed key revokes that phone's keys. Removal, deliberate sign-out and a password change cancel it. The app tries it before the sign-in-again screen. Kill switch on both sides. Rejected: after 1.4.6 (the recommendation), not at all. Design, the same day ("Saboot wala tareeqa"): proof of receipt instead of a timer. The old key stays usable until the new pass is first used, for 7 days at most. Theft cancels keys and passes. 180 days unused ends a key | decided by the owner 2026-09-14 ("1.4.6 mein hi"); server built, design change being made; its table on the owner's word |
| [0102](0102-import-from-contacts-uses-the-phones-own-picker.md) | "Import from contacts" opens the phone's own contact picker (Android's picker, `ACTION_PICK` on older versions, and iOS's `CNContactPickerViewController`). The app receives only the picked contact and stops asking to read the whole address book, which also removes a popup on the way to a first invoice. Google Play makes `READ_CONTACTS` restricted (mandatory on 2027-01-27 at target 37). 0096's whole-book upload cannot use this path. Rejected: after 1.4.6 (the recommendation), keeping the permission | decided by the owner 2026-09-14 ("1.4.6 mein hi"); **built on `VC_102_VN_146` (`8f259399`)**. Same day: older Android gets the name and one number, and the contacts type-ahead goes ("Theek hai, aise hi") |
| [0103](0103-meta-audience-network-joins-admob-mediation.md) | The Meta Audience Network adapter joins the app's AdMob mediation (Android, 1.4.6), so Meta can bid for the app's ad slots. A separate agent tests it on a device. The console side (Meta placements, AdMob mediation groups, Data safety, app-ads.txt if needed) is the owner's, from written steps. The gain (`ad_source`) and the cost (size, start-up, crashes) are both measured | the owner's instruction 2026-09-14; being built |
| [0104](0104-a-payment-date-travels-as-a-calendar-day.md) | A payment's date leaves the app as `YYYY-MM-DD`, like invoices, instead of an instant whose UTC day is the day before for payments entered after midnight in the east. Since 08-28, 107 of 116 payments carried their own day and 9 the day before (not proven to be this). The server reads both shapes and counts them, and ships first. A narrow carve-out from the postponed Payments review: no screen, totals, logic or stored payment changes. Rejected: waiting for the review | decided by the owner 2026-09-14 ("Sirf tareekh abhi theek karo"); being built |
| [0105](0105-remote-screens-are-built-into-146.md) | The owner's remote-screens idea is built into 1.4.6. Screens are designed in the admin panel and shown in the app: phase 1 is the pipe plus a first announcement (server table, panel Screens page with preview, and app blocks), about 3 weeks. The stores allow it if the phone downloads data only and no remote screen asks for a password or payment. Rejected: after 1.4.6 (the recommendation), not now | decided by the owner 2026-09-14 ("Haan, 1.4.6 mein hi"); its table on the owner's word; seven how-decisions to ask |
| [0107](0107-the-premium-screen-promises-only-what-the-app-does.md) | The premium screen lists 12 benefits, and only "No Ads" is real and premium-only. "Auto follow-ups", "Shareable payment links" and "Priority Cloud Sync" exist in no build, and have been listed in every release since 1.3.9. They come off the screen in 1.4.6, with no "coming soon". The chips become Play's "10K+ Downloads", with no rating. Only "No Ads" is shown as Premium, and the rest is listed as free for everyone. The "7-day refund" pill goes. 305 phones saw the screen in 20 days. Play requires subscription benefits to be truthful | decided in four parts by the owner 2026-09-14; built into 1.4.6 |
| [0106](0106-every-date-the-apps-send-is-watched-until-it-arrives-as-a-day.md) | A Health Centre card, "Invoice dates arrive as calendar days", turns red when a build that should send an invoice's or a payment's date as `YYYY-MM-DD` sends anything else: for an invoice every numbered build, every iPhone and the web; for a payment Android 102 and iOS 19 on. It reads Prometheus and adds each new series' first count, which `increase()` misses. An app test pins all six dates on the wire in Karachi, New York and Tonga. Rejected: a tally table, `increase()` alone, judging by `versionCode ≥ 91` (it reads an iPhone as an old build) | decided by the sync agent on the owner's ask, 2026-09-14; built, not deployed |
| [0108](0108-a-correct-refusal-is-a-warning-and-a-fault-stays-an-error.md) | A copy older than the server's is refused at WARN; every other sync refusal keeps its ERROR line. The template chain is complete (4 invoices, all arrived); the largest pager is one account's lines naming products its phone deleted before sending (app 1.4.6 fix proposed). |
| [0109](0109-an-invoice-without-a-client-never-goes-to-the-server.md) | An invoice without a client never goes to the server, and its lines wait with it: one rule (`InvoiceReadiness`) for the orphan scan, the sign-in and the push; the save that adds the client sends it as a create. A nullable client on the server was rejected by the owner. |
| [0110](0110-the-share-loop-events-name-their-link-and-their-door.md) | The share loop's events name their link and their door, with no new event. App: the three `shared_invoice_opened*` / `_open_failed` events carry `entry` = `link_tap` \| `install_referrer` \| `received_list`, all in commonMain, so iOS gets it. Web: every `/i/{token}` event carries `iv_doc`, the raw token, stamped from the request's `Referer`, never the body; it is the same name and value as `install_referrer.iv_doc`. Rejected: a new event, waiting for 1.4.7, an `unknown` value, a hashed token, a new name, a token-scoped route, a per-event backend check. **Addendum (iOS release):** an iPhone's own-app button goes to the App Store (`destination=app_store`); the PDF stays in the browser; one line says to tap the link again after installing, because iOS has no install referrer; Safari's Smart App Banner is on. Rejected: an "Open in the app" button, fingerprinting | decided by the owner 2026-09-15 (app "1.4.6 mein", web "abhi bana do", iOS "aaj ios ki app release"); all built on branches, not merged, not deployed (the App Store part must wait for the listing); backend/panel reading them not built |
| [0111](0111-an-account-can-be-deleted-from-the-app.md) | A signed-in account can be deleted from the app (App Store 5.1.1(v)): it closes at once (every pass refused, share links off, store subscription left to the buyer), a sign-in within 30 days brings it back only on the owner's "yes" (409 `ACCOUNT_CLOSED`), and after 30 days the server erases it: 44 tables listed, with what stays and why. The erase is off until the owner reads the list. Rejected: an immediate hard delete, an email-only request, guests deleting from the route, cancelling the subscription server-side, reopening after 30 days | decided by the owner 2026-09-15 ("Band foran, mitao 30 din baad"); built on branches, not deployed; erase off |
| [0112](0112-an-iphone-buys-premium-through-the-app-store-by-androids-rules.md) | An iPhone buys, restores and keeps premium through StoreKit 2 by Android's rules (0047, 0048, 0070), in shared Kotlin with a Swift bridge. The App Store product ids are Play's own. The server verifies each App Store purchase from Apple's signature (Apple Root CA - G3, pinned), with no shared secret, into the same entitlement tables; there is no schema change. Only Apple's own word is a refusal. App Store Server Notifications V2 is a public route, verified by signature. The Billing card shows the App Store. The paywall offers a retry instead of "Checking plans…". Rejected: StoreKit 1, verifyReceipt, a signature we cannot read counted as a refusal, new product ids, a migration, registering updates blind, `AppStore.sync()` at launch, hiding the paywall | decided by the owner 2026-09-15 for the first App Store submission; built on `feat/146-ios-storekit` (app) and `feat/apple-purchases` (server), not merged, not deployed; App Store Connect is the owner's |
| [0113](0113-sign-in-with-apple.md) | The iPhone app offers Sign in with Apple above Google (App Review 4.8): Apple's native button, a fresh nonce per sign-in; the server checks Apple's token (Apple's keys by `kid`, issuer, bundle id, expiry, the nonce's SHA-256) on the existing social sign-in and then follows Google's path (0053, 0099, 0111). The one-time code is traded for a refresh token after the deleted-account question, and deleting the account revokes it at Apple. The key (.p8, key id, team id) is the owner's, in `.env.prod`. Rejected: a new route, a drawn button, trading the code first, revoking inside the closing, a token without a nonce | decided by the owner 2026-09-15 ("Apple login bana do"); built on branches, not deployed |
| [0114](0114-builds-are-compared-on-an-equal-window-from-each-users-first-open.md) | Funnel Analysis gets a "Version ka muqabla" mode: 2-3 builds side by side on the journey ladder, every first-time device read over the same window (1 h / 24 h / 3 d / 7 d) from its own first open, too-recent devices counted apart, "behind" only past z -1.96 with 50+ devices each; new endpoint `journey-comparison` (ADMIN). The panel's 1.4.7 15 % vs 1.4.6 20 % was half exposure time | built, not deployed |
| [0115](0115-a-panel-read-of-analytics-is-one-pass-and-names-its-plan.md) | Live Events 502 (2026-09-18): `active-users` and `first-invoice-journey` each read `analytics_events` once and pin the plan with index hints (clustered scan for wide ranges, device index for up to 500 devices, `created_at` index for the anchor). 30 d release: user list 23 s + >215 s -> 6 s; journey >150 s -> 14 s; one version 2.1 s -> 1.7 s. Journey no longer drops devices past 2,000 or files them `no_signals`. Rejected: proxy timeout, IGNORE-only hint, always-scan, `/*+ */` hints; rollup/index needs a migration (owner Q1), 5 s poll of 30 d (Q2), 512 MB buffer pool (Q3) | decided (technical); branch `fix/webpanel-journey-502`, not deployed |
| [0116](0116-hold-everything-fixed-and-vary-one-thing.md) | Funnel Analysis' comparison tab becomes "Muqabla": compare by version / country / country tier / install source / ad campaign / platform, every other dimension held fixed, on 0114's equal window and stats. It reports how much of each cohort shares the same days. New `journey-compare` (ADMIN); 0114's endpoint uses the same read; one ladder constant (`JOURNEY_STEP_CASE`). The install source is found from 1 h before the first open, because 322 of 961 referrers are stamped just before it. Meta installs name their campaign only through Meta's encrypted payload (2,414 of 2,415 carry it, stored since July), opened in the backend with the owner's key from `.env.prod`; without the key they show as "locked". Tiers are in `CountryTiers`. 30 d, all builds: 6.1 / 7.8 / 9.8 s at 1 h / 24 h / 7 d. Rejected: Ads Manager URL parameters (none arrive), grouping in SQL, joining the referrer in the window, a brace match, storing the decrypted campaign, folding unknown country into T3 | built on branches `feat/journey-compare-by`, not deployed; key, tier list and the T1 read-again are the owner's (3 questions) |
| [0117](0117-the-admin-panel-signs-in-with-a-passkey.md) | The admin panel signs in with a passkey (email + Face ID, no inbox) beside the unchanged password + emailed code. Yubico webauthn-server-core checks challenge, origin, RP id `admin.invotick.com`, user verification (required) and the counter; the pass is the emailed code's own (`adminSignIn`, same session path). Adding one needs the admin-sign-in pass **and** a fresh emailed code. Challenges on storage (single-use, 5 min, purged); two new tables, migration alone. Sign-in options are public, identical for every email, 10/IP and 300 total per 5 min. Found: the admin sign-in's wrong-password and wrong-code counters roll back (lockout never sets), not changed here. Rejected: remember-device cookie, longer pass, OTP off, challenges in RAM, "recent sign-in" instead of a code, webauthn4j, a browser library, listing passkeys in the options | decided by the owner 2026-09-19 ("Haan, passkey banao"; "Db ki table bhi bana do"); branches `feat/admin-passkey-table` (migration), `feat/admin-passkey` (backend, panel), not deployed |
| [0118](0118-a-wrong-admin-sign-in-is-counted.md) | The admin sign-in's two steps (`adminLogin`, `adminVerifyLoginOtp`) lose `@Transactional`: a wrong password or code was saved, then the refusal's exception rolled the save back, so 3 wrong codes left `otp_attempts` 0, 3 wrong passwords left `failed_login_attempts` 0, and the admin lockout, tiers and hourly code cap never took effect. Now each save commits on its own, as `login`/`verifyOtp` and the passkey steps already did; refusals and the success answer unchanged. App sign-ins checked, not affected. Owner 2026-09-20: the passkey is the never-locked machine; a passkey already added signs in through the password and code locks and leaves them as they were; adding a new one still needs a fresh code, so stays under them. Rejected: `dontRollbackOn` (commits anything before any refusal), `REQUIRES_NEW` (private self-calls skip the proxy; new bean + stale row), counting in the controller, one lock holding the passkey too, a per-IP lock (nginx `set_real_ip_from 0.0.0.0/0` trusts any `X-Real-IP`; proven with 203.0.113.77), an IP whitelist (CGNAT, spoofable). Open: every client IP is spoofable | decided (counting technical, passkey rule by the owner); branch `fix/auth-failure-counters-commit` `d968489` on `feat/admin-passkey2`, not deployed |
| [0119](0119-one-place-decides-which-address-a-request-came-from.md) | One place, `ClientIpResolver`, decides which address a request came from, and a build-failing test keeps every other file out of it: the address nginx saw on the connection, else `remoteAddr`, and `X-Forwarded-For` never again. Found: nginx's `set_real_ip_from 0.0.0.0/0` made a made-up address real (proven with 203.0.113.77), so the passkey's per-IP limit (0117), `lastLoginIp`, the geo lookups and the click log all counted whatever the caller wrote. App, share-link and direct traffic becomes honest; panel and web traffic reports Vercel, so the login history's city and any per-IP limit are Vercel-wide for those two. A shared secret that would have bought the person's own address through Vercel was built and then **rejected by the owner for simplicity** ("mehfooz and simple"), and nothing about safety rests on it: the admin lock is account-wide (0118), the passkey exempt. `ReportedClientIp` (0120) delegates here. Also rejected: trusting Vercel's shared ranges, narrowing nginx alone, per-controller fixes, a DNS lookup of a claimed value | decided (technical; the secret by the owner, 2026-09-20); branch `fix/client-ip-trust` rebased onto 0120's `feat/admin-devices`, panel branch deleted, not deployed; the nginx line is a proposal |
| [0120](0120-the-admin-panel-remembers-every-sign-in-machine-by-machine.md) | The panel's API Access page gets Google's two lists: "Aap ke devices" (one card per machine — label from the User-Agent or the passkey's name, approximate place, last active, login/refuse counts, "Yeh device", and "Is device se sign out" which revokes that machine's passes) and "Security activity" (login, wrong password, wrong code, lock, passkey added/removed, device signed out; 180 days, then deleted). A machine is a random id the server gives the browser in an HttpOnly cookie, so it survives the iPhone losing its storage; it is no pass and decides nothing. Failures are kept too; an email that names no account is kept only as a hash; an app account's failures are skipped. New-device email on by default, with no code and no link. The IP is read through one helper (`ReportedClientIp`), labelled "reported", used for nothing — the header is spoofable (0118). Rejected: a sidebar page, a browser-stored id, grouping by User-Agent or by IP, paid lookups on page load, keeping a stranger's typed email | decided by the owner 2026-09-20 ("setting main login history rakho machine wise", "bilkul jaisy google ky account main hoti hy"); branches `feat/admin-devices-table` (migration), `feat/admin-devices` (backend, panel), not deployed |
| [0121](0121-the-analytics-table-costs-ninety-times-the-bytes-a-panel-read-needs.md) | The three slow panel pages are slow because of how `analytics_events` is stored, not because answers are computed on demand — the owner's own reading, recorded as the frame for analytics-read work. Measured 2026-09-20 on production: journey 17.1 s (742 MB from disk), Muqabla 10.8 s (**3,167 MB**), Live Events 5.0 s every 5 s (671 MB). One Muqabla read needs 34 MB of columns and reads 3,167 MB — **93×**, 971× in page traffic. Three causes, each measured: a random-UUID PK (index-only join 2.23 s vs 9.29 s the moment it needs the row — the fetch is 7.1 s of 10.8 s); a 390-byte JSON blob on a 528-byte row; `build_type`/`ui_mode`/`is_first_open` are VIRTUAL over that blob (one virtual filter 1.32 s vs 0.19 s for a real column over the same scan). Journey time is ~0.5 s per aggregate expression per row, JSON or not, so the lever is fewer rows. Box: 399 MB free, 1.7 GB swapped, mysqld 736 MB swapped against a 512 MB pool, 94 GB/day read from disk. Volume 333/day in March → 41,573/day in September → ~10.5 GB at 180-day steady state. Eight options priced (pool, covering index, drop 3 dead indexes, JSON→real columns, monthly partitions, rollups, cache, ClickHouse). Recommended order: buffer pool 512 MB → 2 GB **+** drop 238 MB of redundant index, then the covering index, then partitions; ClickHouse is over-engineering at 1.72 M rows, right past ~10 GB. Stage 1 does **not** fix the journey (CPU-bound on ~45 aggregates). Rejected: "pre-compute everything like Firebase" (it does not, and a rollup cannot answer tomorrow's question), a derived table for the JSON keys (7.85 → 7.44 s), raising the 500-device threshold (2,301 devices: 16.05 s vs 5.68 s), caching answers in the heap (the 2026-09-04 OOM shape), changing the default range, a new clustered key on its own | measured, nothing shipped; panel quick win on `perf/panel-poll-and-freshness` `1777492` (30-day poll 5 s → 60 s, "computed at" line), not deployed; 5 questions for the owner |
| [0122](0122-who-is-on-the-app-right-now-is-counted-where-the-batch-lands.md) | The Live Events "live now" section stops being a by-product of the thirty-day query: the server keeps the live picture as batches arrive at `/v2/analytics/track` and pushes it over SSE, so a live tick reads no table at all. With the tab open on 30 days: live-count reads 12/min → **0**, user-list reads 12/min → 1 (0121), MySQL ~60 s/min → ~5 s, disk ~8.0 GB/min → ~0.67 GB. The old count was also narrower than it looked — `filter(live)` over a 200-row cap. Held in RAM: one ~300-byte row per device seen in 5 minutes, capped at 1,000 = **under 300 KB**, sized against a measured worst case of 27 devices in the busiest five minutes of seven days; over the cap the oldest go and `dropped` says so. A restart seeds from **one** bounded query and never asks again; until then `seeded=false` means "not counted yet", not "nobody". A second backend instance would each report a fraction — fix before, not after. SSE not WebSocket: a Vercel rewrite carries no protocol upgrade and a WebSocket handshake sits outside `@RequireRole`; `X-Accel-Buffering: no` beats nginx buffering with no VPS change, the stream ends itself at 10 min inside nginx's 900 s, 16 streams max, one loop for all. Panel reads it with `fetch`, not `EventSource`, so the token stays out of the access log; header shows live / live (poll) / rabta nahi. Also fixed: the test JVM now runs UTC like production and CI — a row saved 11:39Z read back through a native query as 06:39Z on a UTC+5 Mac. Rejected: WebSocket, diffs instead of snapshots, token in the URL, counting refused builds, keeping the narrower count as a fallback. 1,229 tests green, no migration. Does **not** fix Safar (17.1 s) or Muqabla (10.8 s) — those need 0121 | built, not deployed; backend `feat/live-now-stream` `0fd4e3f`, panel `feat/live-now-stream` `711c11e`; 1 question |
| [0123](0123-a-filter-the-reader-chose-stays-chosen.md) | Live Events' build filter defaults to **release** (it defaulted to `debug`, which is why every logged request said `buildType=debug`), and every filter on every page survives a reload, Back, Forward and a copied link through one shared hook, `useStickyState`. Order: the URL wins, then what that page was last set to (`localStorage`), then the default. Covered: Live Events (build, version, range, sort, role, live-only, rows, + the reporting table's build/range/sort), Funnel — Pehli invoice ka safar (range, build, version, ui mode) and the funnel query (by, mode, platform, version, os, country, city, max step minutes), Users, Inventory items, Screen flow, Users map, IP stats. Read from `window.location` and written with `history.pushState`, not `useSearchParams` (a Suspense boundary on every static page for nothing) and not the router (a remount would drop 0122's stream); setters batch into one history entry per microtask, so two filters moved together fire one fetch. Free text stays out of the URL and out of storage — a search box here holds an email. Verified in a real headless browser, 14 checks all passing: first visit, URL written on arrival, change reaching URL + storage, reload, Back, Forward, a shared link beating memory, one history entry for two filters, a date range round-trip, a junk value ignored. Rejected: `useSearchParams`, `router.push`, one opaque packed parameter, storage-only, URL-only, remembering the search text, per-page copies | built, not deployed; panel `feat/live-now-stream` `711c11e`; 1 question |
| [0124](0124-an-app-open-ad-is-asked-for-only-when-somebody-is-there.md) | The owner's phone kept asking for app-open ads after the app was closed: a WorkManager wake started the process every 15 minutes, `MobileAds.initialize`'s callback called `preload()`, and the request went to a process with no screen in it. Measured 7 d to 2026-09-20, Android release: **5,976 of 18,487** app-open requests (32.3%) had no foreground — 293 phones, 20 each, 315 at worst; AdMob filled 2,511 and **one** was ever shown (**99.96% wasted**, against 43.3% where a person was behind it). Already fixed — `everForegrounded` shipped in vc97 and every one of those rows is from build 92 or 94 — so this makes the rule survive: `AppOpenRequestRefusal` holds `preload()`'s seven checks in `preload()`'s own order (as `show()` does with `AppOpenShowRefusal`), and a build-failing test catches a new request call site, a `preload` that stops asking, or a lowered latch (proven red). The offline refusal is **built and OFF**: "a request with no network cannot fill" was refused by the data — of 897 requests on builds 101+ reading `net_online=false`, **220 (24.5%) were shown**, earning $0.62 of the week's $26.32 (2.34%), so switching it on today costs real impressions. `skipRequestWhenOffline` is false in the bundled default and the parser, pinned by two tests; instead every `ad_request` carries **`had_network`**, ConnectivityManager's live reading, which in a week says whether the switch is free. Showing is untouched — an ad fetched online still shows and pays offline (321 of 5,438 `ad_shown`). Rejected: a per-day request cap, moving the preload to ON_RESUME, a hard offline refusal, guarding the splash's own request, an `app_open_decision` per preload skip (+6% rows) | branch `feat/146-app-open-requests-need-a-person` `20e832ea`, pushed, not merged; 620 tests green, no schema change, no version bump; 1 question, and it waits a week for its number |
| [0125](0125-a-sync-failure-is-still-only-a-refusal-and-it-is-counted-once.md) | The owner's three `sync_failed` classes from one phone (Invotick ID 903623205), each read back from the rows rather than the dump. **A — the ownerless queue:** nothing was lost; the queue adopts an ownerless row before every push, and the product's delete did reach the server (created 09-06 00:20 UTC, deleted on the server 09-09 00:55:40 by that same phone). Fixed in 1.4.5 for products — 135 rows on 49 phones, all build 94/97, **0 on 101+**. Still live and fixed here: `updatePayment` blanked `payment_instructions.userId` (the screen's object carries no owner), so the next edit queued ownerless and the orphan scan — which also reads `userId` — could not see the row either; the delete used a hard-coded `""`. **B — the STALE_CONFLICT after the share:** not the share and not an invoice. `315e8a2d` is a **client**, written by that same phone, microsecond stamp = the server's clock: the pre-2026-09-11 clock bug (rule 3), already fixed server-side; version 2 is the phone's own edit, so nothing was lost. What is fixed is the double count — STALE_CONFLICT sits on the non-retryable list too, so one refusal was stopped and reported twice; 30 d on 101+ the pairs match exactly (206/206, 70/70, 21/21, 12/12, 7/7, 1/1), plus 360 stale CREATEs counted as dropped although the code had just re-queued them as updates. 0029's last open line closed. **C — the offline rows:** pure network, and the rule has shipped since **1.4.5** — on builds ≥ 101 **not one** `push_failed`/`pull_failed` carries network wording; this phone is on 94/97. The one place the rule never reached is `guest_auth`, on the shipped build: 364 of 379 rows in 7 days from 118 phones, because `GuestSignInFailed` names the thrown class as a string, not a cause. No harm proved first: 113 of the 118 phones have a server row, the other 5 stopped sending any event within 4 minutes. Retry, attempt count and the backup-blocked warning all stay. Numbers: 7 d, 38,258 rows / 1,197 phones; on a current build the comparable remainder is 3,289 (after 1.4.7's own `push_gather` fix removes 26,497) and this change takes out **1,048 (32%)**. **Owner, 2026-09-20: all three confirmed.** (1) Offline reporting off — a phone with no network and a guest sign-in that never reached the server stop being counted; retries, the attempt count and the backup-blocked warning stay. (2) Old builds' offline rows left alone. (3) The payment-method delete is a separate task, **not started** — a handover section in 0125 names what is broken, the server half that must ship first (the full pull must send a deleted payment method a live row names, as `f8a6722` does for businesses and products), the risk to a second device if the order is reversed, and the open in-use question. Rejected: filtering in the panel, dropping the divergence class, silencing `guest_auth` entirely, deleting old builds' rows at ingest, shipping the payment-method delete without the full-pull half, `git stash` | decided by the owner 2026-09-20; app `fix/147-sync-failed-only-real-refusals` pushed, 333/333, not merged, ships with the next release; nothing to deploy |
| [0126](0126-the-buffer-pool-grows-online-and-the-backend-gives-what-it-can-spare.md) | The owner's 0121 Q1 answer, prepared and partly refused. **The pool resize needs no restart at all**: `innodb_buffer_pool_instances=1`, `chunk_size=128M`, MySQL 8.0.46 → `SET GLOBAL innodb_buffer_pool_size` is online and background. **1.5 GB from the backend is refused on measurement**: Prometheus 30 d says peak live-data-after-GC **994 MB and rising** (517 MB a fortnight earlier), peak heap used 1,979 MB; `MaxRAMPercentage=75` makes a 1.5g container a 1.125 GB heap — 131 MB over the live set — and `-XX:+ExitOnOutOfMemoryError` means the JVM **exits**, taking sync, the panel and the share-link mint down. Safe number is **2.5g** (heap 1.875 GB, 1.9× live), freeing 500 MB. The other 1 GB is not on the box: a CI build takes **1.40 GB + 427 MB test MySQL** on every push, and during one today free RAM was **176 MB with 1,424 MB in swap** and mysqld already **535 MB swapped** — a bigger pool would simply swap. So: pool 512 MB → **1 GB** first (from page cache, costs nothing), 2 GB only after the runner leaves the box. `docker-compose.yml` must change **in the repo** — the deploy scps it over the VPS copy. Full backup/change/proof/rollback runbook; quietest hour 00:00 UTC (6,007 events). **Q2 answered: drop all three indexes** — `_event_name` re-measured against `_name_ts` at 0.80 s vs **0.69 s** and 0.51 s vs **0.31 s**, so the superset is no slower; 238 MB and ~160,725 index writes a day back. **Q3 prepared alone**: `migration/analytics-covering-index` `87f9e35`. **Q5 answered: the purge runs** (232 rows at 10:26 UTC, oldest `event_timestamp` = the cutoff exactly); the "199 days" was read off `created_at` while retention deletes by `event_timestamp`. Two new findings: **503 rows are future-dated and can never be purged**, and the purge cap is **10,000/day against 53,575/day intake**, which breaks about **March 2027**. Rejected: a restart, 1.5g, 2 GB today, editing compose on the box, taking RAM from monitoring/jariya, separate migrations | prepared, **nothing applied, nothing deployed**; `infra/backend-memory-limit` `2b6573d`, `migration/analytics-covering-index` `87f9e35`; 5 questions |
| [0127](0127-an-event-row-says-which-build-sent-it.md) | Every Live Events row names its build — `1.4.2 (94)` — on screen, on **every** exported line, and in the export header; a device whose rows span builds gets `# build: MIXED — …. Read every line's own build before comparing them.` The owner exported a feed on 2026-09-19 that said only release/debug, and a day went into measuring behaviour that belonged to 1.4.2 (94) and had been fixed two releases earlier. `LiveEventDto` gains `appVersion` + `appVersionCode` off the event the feed already loaded — no second query, no index, no extra column read. Two fields because only the integer orders (`1.4.10` sorts below `1.4.9` as text); null prints an em dash and is never guessed (web rows, oldest app rows). The build sits on the first line beside the time, not in the params blob. The event-totals Copy button gains a header block (build, versions, range, copied-at) — it had none. `buildStreamReport` and helpers move to `lib/streamReport.ts`: the pasted copy is the one that gets believed, and a pure function can be run and read. `AFeedRowNamesItsBuildTest` proven red first; 1,231 tests green; exporter verified in a real browser for one-build and mixed-build, sample in the decision. Not a defect, recorded: the first probe looked reversed because the page holds the feed newest-first and the exporter reverses it. Rejected: the version in the params blob, name without code, filling a missing version from the session, header-only, a separate query | built, not deployed; backend `feat/live-events-show-build` `776506b`, panel `feat/live-events-show-build` `43b6571` (stacked on `feat/live-now-stream`); 1 question |
| [0128](0128-a-payment-method-deleted-on-the-phone-reaches-the-server.md) | A payment method deleted on a phone never reached the server: `PaymentInstructionRepositoryImpl.deletePayment` and `deletePayments` — the single and multi-select deletes on the Payment methods sheet — soft-deleted the local row and **queued nothing**, and set no `syncState`, so the orphan scan (which reads `PENDING_*` rows only) could not catch them either. Production, 2026-09-20: **240 payment methods on the server and not one ever deleted**, the exact shape the client delete had before 0068. **83 of the 240 are in use** (59 live invoices, 34 payments, 1 estimate), 152 are named by nothing; 31 accounts own more than one; exposed today: **2 accounts, 9 invoices** (two live devices and invoices naming a payment method). **Server half ships first**, because `invoices.paymentMethodId`, `estimates.paymentMethodId` and `payments.paymentInstructionId` are each a foreign key on the phone: the full pull now sends this account's deleted payment methods that a row it sends still names, as deleted rows (`f8a6722`'s shape, which already covers businesses and products), bringing the business with them, never another account's, delta unchanged, **no migration**. Reversed, a second phone gets an invoice naming a payment method it can never be sent — up to 1.4.5 it cannot store the invoice at all, on 1.4.6+ it is a missing-reference report on every pull (class P and the 0068 family repeating). App half: soft delete **plus** a queued DELETE under the row's own owner, in one transaction, the row left `PENDING_DELETE` so a lost queue row is recoverable; a **pulled** delete uses `markDeletedByServer` (SYNCED) or the phone sends the server its own delete back; a deleted payment method this phone never held is kept, deleted and SYNCED (`applyServerDelete`), like a deleted business, client, product or terms. One named behaviour change: a delete not yet sent is now unsent work, so a later edit elsewhere no longer silently revives the row. **Owner, 2026-09-20 — SETTLED, do not reopen:** *"Payment method ager kisi invoice per use hy and usky baad delete krain to invoice my wo payment method delete nhi hona chahiye, and payment method bhi aik soft delete ky tour per hoga."* So an in-use payment method **may** be deleted (no `ClientInUseException` twin — he was shown 83 of 240 in use, 152 free, 31 accounts owning more than one), the delete is **always soft**, and a document that already carries it **keeps showing it**. Checked path by path, not assumed: saved-invoice screen, preview, edit, the offline HTML snapshot, the PDF and share image all read it through the one unfiltered `@Relation` on `paymentMethodId` + `InvoiceWithRelationMapper`; the server's REST/webpanel/pull reads use a plain `@ManyToOne`; `/i/{token}` and `/embed/render` read the **frozen** `shared_invoice.snapshot`, so a later delete cannot reach an already-shared link (invariant 4); estimates the same; the **picker list alone** filters `isDeleted`, which is what it must do. Soft delete named exactly — phone `payment_instructions.isDeleted` + `dateDeleted` + `PENDING_DELETE`, server `is_deleted` + `deleted_at`; no `DELETE FROM payment_instructions` exists in the backend, and the phone's two **unused** hard deletes (`delete(entity)`, `deleteByIds`) are removed, because `ON DELETE SET NULL` on all three links is exactly the trap that broke client deletes before 0068. Rejected: shipping the app half first or alone, **refusing an in-use delete (the owner said no — do not re-propose)**, clearing the link on the document, a hard delete, marking the shared `softDelete` and letting the pull use it, queueing outside the transaction with no state on the row, folding deleted **templates** into the same change (the other half `f8a6722` missed — noted, separate), repairing anything on the server (nothing is wrong there; the deletes simply never arrived) | built both sides, **nothing deployed, nothing released**; backend `fix/full-pull-sends-deleted-payment-methods` `0e84fac` (1233/1233, 2 of 3 first cases red first), app `fix/147-a-deleted-payment-method-reaches-the-server` `f51d81ca` on `fix/147-sync-failed-only-real-refusals` (350/350, 6 of 9 red first + 1 that did not compile; the owner's-rule guards pin what was already right — proved they bite with a reverted filter probe); both pushed, no migration, no version bump; **no open question** |
| [0129](0129-a-phone-that-thought-it-was-online-and-could-not-reach-us-is-evidence.md) | The owner's second device (Invotick ID 941940223, 1.4.4, Bangladesh) failed to resolve `stage.invotick.com` four times in seventy minutes with **`net_online=true, net_type=wifi`** on every row. **His question — would he ever see it on /health — answered: no.** `SyncFailureCheck` decides "the device's surroundings" from the reason text, and `Unable to resolve host` reads the same either way, so every such row was counted into "Ignored — the device had no network" and left out of the verdict (card: 7-day window, CRITICAL at 10 devices or 200 retries on one, Android from versionCode 94); on the drill-down they name no phone at all (`device_id` NULL on all 1,873); and from **1.4.5 the app stops sending them entirely**, so the evidence was about to vanish. 30 d: of the rows carrying that message, **14,716 on 1,270 phones read offline and 8,343 on 1,007 read online** (pull 6,671 / push 1,675; wifi 3,818, cellular 3,942, VPN 586; builds 91–100, none from 101). **Ruled out:** an ISP/DNS block (every day of the window, up to **70 countries in one day**), a phone cut off for good (**993 of 1,007 reached the server again**; the owner's own is now on 1.4.7 and had 0 invoices), and a naive online flag (both readings already require Android's `NET_CAPABILITY_VALIDATED`). **What is left is that nobody asks at the moment it matters:** the gate and the event stamp both read a cached `StateFlow`, and that phone had been parked in the background for 1.8 hours. Fixed: `isCurrentlyConnected()` asks the system and corrects the flow, the sync gate asks instead of remembering, and a call that never arrived takes a fresh reading — offline stays silent (0125), online is reported as `push_unreachable`/`pull_unreachable` with 0050's own columns; the card files those two stages on their own honest line, shown and never counted (`app_stage` already holds them, **no migration**). **Owner, 2026-09-20: the card GOES RED.** Red is a rate and means "something got worse": at least **25 phones** *and* **8 % or more** of the phones we heard from that week, both as properties (`health.sync-failures.unreachable-red-share-percent`, `…-red-devices`) so a wrong first number is a config change, not a release. **8 % is measured, not guessed** — 347 of 4,292 phones in the week to 2026-09-20 is 8.1 %, and that reading came from a cache that could be hours old, so it is the worst the number can be; once the phone reads live it can only fall. 25 phones is 0.6 % of an ordinary week, a floor so a quiet day cannot shout. Denominator: one bounded `COUNT(DISTINCT device_id)` over 5,915 `linked_device` rows — answerable only for a window ending now, and "not known" means nothing goes red. The card names phones, occurrences, the share and the **builds**; country and network are not claimed, because the row does not carry them (that would be a migration). During rollout the share reads low, so the card under-reports rather than crying wolf. **Dated follow-up in 0129: re-run the four queries a week after the release and set the number from them.** Rejected: an in-run retry, counting them as refusals, filtering server-side, a new page, trusting the old `net_online`, a raw count as the threshold, a previous-window baseline from `sync_failure` (its rows accumulate, so the comparison would invent a rise), a hard-coded threshold | decided by the owner 2026-09-20; pushed, not merged, **not deployed**; app `fix/147-a-phone-that-thought-it-was-online` (332/332), backend `fix/a-phone-that-could-not-reach-us-is-named` **`3f9d4ee`** (1238/1238) |
| [0130](0130-one-drift-is-one-report-and-a-guests-stranded-work-is-named.md) | The owner's Live Events screenshot of one guest phone (Invotick ID 926717497, PK, **1.4.7**) "flooding" with sync failures. **It is not a flood and not new:** that phone has sent **145 `sync_failed` ever and exactly 3 on 1.4.7**, all the same `reconcile_drift` — *"invoiceItems 18 missing, inventoryItems 4 missing"*, unchanged since 2026-09-09, **23 times in 7 days**, once per reconcile pass. Its real volume is `app_cold_start` **5,378**, every one from before the build number existed, none since 2026-08-23. The jumping times are the panel listing by arrival and printing the phone's own clock (PKT = UTC+5) across different days; arrival trails the stamp 15–18 min on every row, which is the analytics flush — **not a clock fault**. **Why the number can never fall, and the harm:** all 22 records were refused `OWNERSHIP_VIOLATION` — 4 products whose ids belong on the server to **another guest** (`a3c112fb`, created them 2026-05-02) and the 18 invoice lines pointing at them — so **6 of this guest's 7 invoices sit on the server with zero lines**, no items and no amounts (G3). Fixed: `reconcile_drift` says its sentence **once**, and again only when it changes, remembered per user and across process starts (`DriftReport`), and it now carries **how many operations the server has refused for good** (`sync_queue` TERMINAL), counted not matched, so a standing-still drift stops reading as work in flight. Fleet, 7 d: **41 rows / 13 phones → 11 distinct facts**; this phone 23 → 1. **None of the other three branches touched drift**, so all 41 would have survived them. **Also answered: the panel's "151 users report no build type" is not an app hole** — 280 rows / 187 ids in 7 days, **every one `platform=Web`** (the share-link page, which by 0045 sends no build and no version code); it is one panel filter, left to the agent working there. Rejected: a timer instead of the fact as the key, in-memory only, leaving the refused count out, subtracting refused rows from a per-entity count, blaming the clock. Open: **76 of 4,326 phones use their own guest id as the device id** (1.8%), so on those a new guest reads as a new phone | built, pushed, not merged, not released; app `fix/147-one-drift-one-report` `c73b6da3`, 329/329; **1 question — recover the guest's 22 stranded records?** |
| [0131](0131-the-live-stream-says-it-is-not-compressed.md) | The live-now stream answers `Content-Encoding: identity` beside its `X-Accel-Buffering: no` and `no-transform`. A compressor holds bytes until it has a block, and this stream sends a few hundred bytes every couple of seconds — so the browser gets the headers, believes it is connected, and receives **nothing**, while the page's indicator stays green because it is set when the headers arrive. Measured 2026-09-20 through a gzipping proxy: **0 frames in 8 s, indicator "live"**; answering `identity`, every frame and the list row in **33 ms**. Production does not compress this route today — which is why the owner's header could say "5 live" at all — so this was **not** his bug; it is one proxy setting away on a path with two proxies. `no-transform` alone was already being sent and was not enough. Rejected: relying on `no-transform`, turning gzip off at nginx (one proxy, a VPS change, undone by a tidy-up), padding each message. `ALiveStreamIsNeverCompressedTest`; 1,244 tests green | built, not deployed; `fix/live-stream-is-never-compressed` `ded2bce`; no questions |
| [0132](0132-the-live-list-arrives-with-the-live-count.md) | The owner saw "5 live" above "No matching active users". Reproduced exactly, then fixed: the pushed stream's devices are merged into the user list and the live number is counted **off the rows about to be drawn**, so header and table cannot disagree. A row now appears in **33 ms** instead of never (>15 s). Three causes, one mine: (1) the count is instant while the list is a query whose beat 0121 had just slowed 5 s → 60 s, so rows arrived 60–65 s old and "Live only" — which keeps only rows inside 60 s — threw away nearly all of them, permanently; (2) two definitions on one line — devices/1 min/unfiltered vs users/30 days/filtered; (3) a device with no user id is countable but not listable (`user_id IS NOT NULL`), measured at 1 of 66 devices in an hour, and it is the newest users, since the cold start fires before a guest session restores. **No new query**: the stream already carried the devices (200 cap, 1,000 registry, 0122), so reads/min stay 1 for the list and 0 for the live half. Now labelled `N live (pichhle 1 minute)` and `M (22 Aug → 20 Sept)`, with live phones the filters hide said out loud. A user-less phone is listed but not clickable, with the reason. No flicker: stable keys, a 2.4 s tint that never changes row height, `prefers-reduced-motion` honoured. Verified in a real browser against a stub (no admin credential here — stated, not glossed). Rejected: re-fetching on every pushed change (5 s / 671 MB per arrival), putting the beat back to 5 s, a second list, widening "live", dropping device-keyed rows | built, not deployed; panel `fix/live-list-arrives-with-the-count` `7c02d09` on `feat/live-events-show-build`; 1 question |
| [0133](0133-a-record-the-server-already-holds-is-not-created-again-and-the-audit-behind-it.md) | The owner, 2026-09-20: *"isko permanently fix kia hy kia tm ny? … sab ka solution banao."* **Now yes — and no id ever collided.** Every id is a random UUID; what happens is that **one id is offered twice under two owners**: a phone's offline guest registers, the server returns its own user id, `migrateUserId` re-owns every local row **keeping its primary key**, and `prepareForSyncAfterMigration` resets all 21 tables to `PENDING_CREATE` and queues a fresh CREATE — on the written assumption *"the real server account has no knowledge of any of these records"*. True the first time only: a database that survives a change of identity (a second offline guest, a restored backup) still holds rows the server accepted under the previous owner, and every one is then refused `OWNERSHIP_VIOLATION` for ever. **Fixed at the root:** the 21 reset statements now ask `lastSyncedAt IS NULL` — never-seen rows are queued exactly as before, already-acknowledged rows stay SYNCED (not queued, not orphan-scanned, the other owner untouched, G3), and the count that stays is reported once as `queue_previous_identity` so the work is not dropped in silence. **App only, no server change, no migration**; one change covers all 14 affected entity types. **Counts (read-only):** 75 records on 30 phones, 293 refusals, first seen 2026-07-19, **still arriving on 1.4.7**; 19 of 22 refused invoice ids exist under **17 other owners**, 12 of 15 line ids under 12. **Stranded work:** of 32 accounts that reported it, 9 empty invoices — 6 of them one guest (6 of its 7), then 2 and 1; **the 6-of-7 ratio is the worst case, not typical**. Context, not claimed as this defect: 1,341 of 8,774 live invoices (15.3%, 467 accounts) have no lines, from several causes. **Audit table of every sync class** with measured sizes and verdicts: STALE_CONFLICT (clients 642/557 phones — the clock bug + the receipt number), INVALID_REFERENCE (lines 51 records / **27,831 occ**), unreadable bodies (invoices 2 records / **70,241 occ**; estimates 17 / **44,226** = 0056, built and not deployed), `updatedAt` missing, invoice-with-no-client (0109), NOT_FOUND_ON_DELETE (0059, correct), ownerless queue (0125), a delete that never travels (0128), seeded ids (safe by design). Recommended order: deploy 0056 → re-measure INVALID_REFERENCE after 1.4.7 → the 2 unreadable invoices → the receipt number → recovery. **Recovery of the 75 already-stranded records is NOT built** (re-issue ids, re-point children, one transaction). Rejected: letting the server accept another owner's id, auto re-issuing now, making ids collision-proof (nothing collided), filtering later in the push, deciding from `syncState` | root cause fixed; app `fix/147-a-record-the-server-already-holds-is-not-created-again` `d2f35deb`, 327/327, pushed, not merged, not released; **1 question — re-number the 75 after the release?** |
| [0134](0134-ci-takes-what-is-idle-and-spends-the-free-minutes.md) | **Correction first:** the "157 % CPU" I reported for a CI build was `ps`'s cumulative average; the build container is pinned to `cpus="1"` and was measured mid-build at **48 % of one core, 1.156 GiB of 3 GB**. CI never took both cores. What *was* missing: the **test MySQL service had no limit at all** (`Memory=0`, `NanoCpus=0`) beside a capped build → `service_memory 768m` / `service_cpus 0.5`; **nothing gave production priority** (every container at the default weight) → `cpu_shares=128`, about **8:1** for production while CI still gets its whole core when the box is quiet — a weight, not the manual up/down the owner described, because a weight needs nobody to remember it; and **MySQL runs on the host** so no Docker limit reached it (`CPUWeight` unset, `MemoryMax=infinity`, measured with **535 MB in swap**) → systemd drop-in `CPUWeight=10000`, `IOWeight=1000`, `MemoryLow=1600M` (a soft floor, deliberately no `MemoryMax`). Build cost estimate: unchanged on a quiet box, ~1,088 s → ~1,150–1,250 s while production is busy; an OOM at the cap is a **red pipeline**, which is why 3 GB is left alone. `io.weight` **not** done: kernel has mq-deadline, it needs BFQ. **Free minutes:** the namespace is Free with 400 min/month and had used **ZERO** — every job pinned to the VPS — while running 126 pipelines in 30 days. `choose-runner` (on the VPS) reads namespace usage and sets `$RUNNER_TAG`; only `test` moves, worth ~22 test jobs a month, **one pipeline in six** — free, not a fix. A **60-minute margin** stops a pipeline starting on shared runners without room to finish, and any error falls back to the VPS. Checked and cleared: deploy SSH key (CI variables, so portable — kept on VPS anyway), registry push, MySQL service, cache (none), VPS-only secrets (none). Also recorded: **`/etc/gitlab-runner` does not exist on the host** — the live file is `/srv/gitlab-runner/config/config.toml`. `/ci/lint`: valid | prepared, nothing applied; `ops/ci-runs-at-low-priority` `46bf206`; 2 questions |
| [0135](0135-what-belongs-on-the-owners-mac.md) | The owner's M3 Max (36 GB) runs the full backend suite in **147 s** against the VPS CI job's **1,088 s** median (1,315 s worst) — about **7×**, honestly 5–7× once CI's cold clone and cache are allowed for. So: **(a) a fast local dev/test loop on the Mac — yes, this is the whole win**, ~2 minutes to an answer instead of ~18, no VPS load, no minutes spent; note Docker Desktop is currently set to **2 CPUs / 5 GB** on a 14-core/36 GB machine, i.e. the VPS's core count, and raising it is one dialog. **(b) the Mac as a CI runner — no:** the Dockerfile **compiles inside the image**, so an amd64 build on arm64 would emulate the whole Gradle compile (I could not get a trustworthy emulation multiplier from micro-benchmarks and therefore quote none); it only works while the laptop is awake and online, and the owner works from his iPhone; and `stage` is production, where a half-run pipeline already cost an older image shipped over a newer one on 2026-08-23. **(c) production on the Mac — no:** residential IP and reputation, no uptime commitment, TLS, the domain, backups and 04:00 jobs. Found on the way and worth doing regardless: the jar has **no native dependencies**, so splitting the Dockerfile (build the jar natively, then `FROM eclipse-temurin` + `COPY app.jar`) removes the architecture question entirely and speeds up every build on any machine. Rejected: the Mac as a runner, quoting an emulation number my benchmarks did not actually measure, production on the Mac | an answer, nothing built; 2 questions |
| [0136](0136-a-stranded-record-gets-a-new-number-so-the-work-can-be-sent.md) | The owner, 2026-09-20: **"Abhi bana do"** — the recovery ships in the **same release** as 0133's prevention, not after. A record the server refused **for good** because its id is another account's gets **a number of its own**, its children move onto it, and it is sent as an ordinary create. **The state is exact:** the queue's `TERMINAL` verdict *with* the server's `OWNERSHIP_VIOLATION` — never a record merely unsent, quarantined, or failing for another reason, and never a guest's record waiting for its work to join an account (rule 24), which resolves itself. **Safe to interrupt by construction, not by a marker:** one Room `@Transaction` per record, in the order the foreign keys demand — insert the copy under the new id, move the children onto it, delete the old row last; no line ever points at an id that does not exist, and anything that throws rolls the record back. **Safe to run again:** the old id is gone afterwards. The copy is the entity's own `copy(id = …)`, so no column can be forgotten. **The other owner's row is never read, written or looked up**, and the server is never asked (G3). **Duplicates:** the pull only sends this account's rows, so the old record cannot come back here; if it ever did it would be one account's own visible duplicate, never another account's data. **Reports once per run**, `sync_failed stage=queue_renumbered` with the count and kinds, so the recovery is countable after the release. **Re-measured: it rescues 52 of the 75 (69%)** — invoices 22, lines 15, clients 9, products 2, payments 2, estimates 1, estimate lines 1 — i.e. every kind carrying real user work. **23 out of reach and said so:** stamps 6, templates 5, signatures 4, headers 3, payment methods 3, terms 1, businesses 1 — their ids also live inside `invoices.presentationJson`, so moving one means rewriting JSON; a business is named by nearly every table. Boundary named: a freed document can still be refused for a reference to one of those 23. Rejected: changing the id in place (the FK is checked per statement), a "being renumbered" marker, an explicit column list, renumbering the JSON-referenced assets, asking the server, one row per record | built, pushed, not merged, not released; app `fix/147-a-stranded-record-gets-a-new-number` **`65b6d9c2`** on 0133's `d2f35deb`, 334/334, 4 of 7 cases red first |
| [0142](0142-the-journey-is-kept-per-device-and-checked-against-the-events.md) | The owner, 2026-09-21: option B — *"ye kaam ek hi dafa kar lein to future mein sab jagah aasani aur fast"*. **Data in the shape the questions need, never pre-computed answers:** `device_journey` (one row per first-open device: first open frozen at its cold start; the first moment each rung of `JOURNEY_STEP_CASE` was reached within 7 days; first share, first country, install referrer; tier, campaign and eligibility worked out when read) for Muqabla, and `device_journey_hour` (the journey's own `JOURNEY_AGGREGATES` per device × arrival hour × build) for Safar, because **one row per device cannot give Safar's answer**: 791 of 5,163 devices have events on another build and 130 have events before the range. Kept true by a touch **after commit** in its own transaction (never costs a phone its events; proven with the table renamed away), a 20 s refresher that recomputes only the hours waiting, a 10-min sweep, and a **daily check** (8 days, corrects and counts; Health Centre card `device-journey`: warning > 0.5 %, critical > 2 % / 36 h / refresher > 15 min behind). Backfill = the first sweep, whole history of the flag (since 2026-07-27), nothing lost beyond 30 days. **Proven identical on production** (shipped SQL on TEMPORARY tables, read-only, release, 30 days): Safar 5,175 devices, **0 differing** on 44 fields, **15.3–19.2 s → 0.8–1.4 s**; Muqabla 24 h 5,174 devices, **0 differing**, **2.5 s → 0.08 s** (61 double first-opens read from the events). `readFrom=events` keeps the old read one parameter away; pages fall back by themselves while filling or behind. **Live Events list:** covering index worth ~6.5 → ~2 s (+150 MB), not recommended; a per-user table would be instant. Rejected: one row per device for Safar, device-day rows, increments on ingestion, whole-device recompute per batch, writing inside the ingest transaction, reading waiting devices stale, storing tier/campaign, a new page | built, pushed, **not deployed**; backend `migration/device-journey-tables` (`cf6c1dc`, owner's word) → `feat/device-journey-kept-true` (`8761736`) → `feat/device-journey-read` (`1bd2e21`), panel `feat/journey-says-where-it-read` (`451a0a5`); 3 questions |
| [0143](0143-a-purchase-follows-its-phones-newest-account-three-times-a-year.md) | The owner, 2026-09-21: *"latest UUID ko premium assign ker dain … 3 latest UUID … 4thi per deny"*, in the safe shape he chose. **One place decides** (`PurchaseMovePolicy`), only after the store confirmed the purchase in the same call (rule 4). A closed account's purchase never moves (rule 11). **3 moves in any rolling 365 days**, counted from `entitlement_binding_log` (from ≠ to) after the latest support reset; the 4th answers `MOVE_LIMIT_REACHED` with the date it becomes possible, and the purchase stays. It **moves by itself only from a guest on no other phone** than the one asking — **correction to the premise: a guest can have a second phone by device link**, so such a guest is asked like a signed-in account; everyone else needs the user's yes (`moveConsent=true`), else `MOVE_NEEDS_CONSENT`. **Only to the account most recently JOINED to the phone** (`linked_device.first_seen_at`), not the one open now, so A → B → back to A = 1 move (clarified with 0146). **Builds in people's hands send no `moveConsent` and get exactly today's answers** (tested, 13 new tests red on stage's behaviour). **Found and closed:** the purchase report moved a purchase silently from anyone, including a closed account — 2 real moves in production, both from guests. App: one question ("Move Premium to this account? … This phone stays Premium either way" — Move here / Not now), "Not now" quiet 30 days, the refusal told once, both platforms from shared code. Support reset in **Billing Health** (reason required, written to the binding log as `MOVE_COUNT_RESET` with `admin:<id> <why>` in `device_id` — no migration). Health card warns on accounts refused a 4th move. Off switch `billing.purchase-moves.enabled`. Rejected: moving from anyone by itself, 3 for life, auto-move for old builds, keeping the report's silent move, every guest counted alone, asking every launch, never asking again, a note-column migration, a new admin page, counting from `account_binding_count` | decided; backend `feat/purchase-moves-to-the-latest-account` `5d3d06d` (1,331/1,331), app `feat/148-premium-moves-with-consent` `e0d42101`, panel `feat/purchase-move-reset` `e641466`, **not deployed**. Server can go now; app with the next release |
| [0144](0144-a-reinstalled-phone-shows-the-accounts-it-used-and-a-guest-comes-back-only-with-proof.md) | Designed 2026-09-21 on the owner's ask, one question open. After a reinstall, **after the splash** (never on it), the first screen lists the accounts this phone used before — masked Invotick ID, kind, invoice count, last day; no name, logo or email. Signed-in → normal sign-in. **A guest comes back only on this phone's id plus one question the real owner knows** (a business or client name; 5 tries a day). iOS id moves to the Keychain (this device only), copied from the current id. Data: 32 guests with 212 invoices left behind on 24 phones. Rejected: the id alone, names/logos on the rows, a picker that holds the splash. |
| [0145](0145-a-phone-linked-to-a-guest-is-a-guest.md) | The owner's test on 1.4.7, 2026-09-21: a phone joined to a guest by QR showed "User", no Invotick ID, "Add Business" and Sign Out. **1.4.7 took every link for a registered account** (29 of 41 links ever joined a guest). Built: the claim's answer says the `role` (and a registered account's email and name); the phone becomes a guest session for a guest, keeps the Invotick ID at once, and a 1.4.7-stored link is restored as the guest it is; a stored business the account does not hold is replaced by its first; the claim names its phone; an iPhone asks as `IOS`. Rejected: drawing only the guest header, reading the role only from the pass, a server-only fix for 1.4.7. **The owner chose (a):** a linking phone is asked about its own work first; yes joins it through 0053's claim (a guest account may take it only from a phone linked to both; server first), no keeps it reachable from Linked Devices. **Open:** one of two guest phones creating an account strands the other; 0086's bookmark. |
| [0146](0146-several-accounts-on-one-phone-each-in-its-own-database.md) | **APPROVED 2026-09-21 — build now, ship with everything else** (the owner: *"abhi … 1.4.8 hi consider karo"*, then *"sab 1.4.8 mein, 4 hafte baad"*; the recommendation to wait for 0144's count was not taken). Proposed the same day on the owner's ask for an account switcher in the drawer. Count (90 days, read-only): 134 of 5,757 phones used 2+ accounts, but that is reinstall churn (guest → new guest 96) and sign-ups (77); **1 phone used two signed-in accounts, and 0 used two accounts with work in turns**. 173 accounts already hold 2+ businesses (the business selector). **One database per account** (`invotick_v2.db` kept for the account that owns it, a new file per new account, a Koin scope per account), a guest's pass kept on the phone (0144's proof only for a guest the phone lost), jobs bound to an account, analytics flushed before a switch, push tokens per account (77 tokens already sit on 2+ accounts). **Found:** 0143's launch restore would spend a purchase's 3 moves on switches — "newest account" must mean newest to join the phone. Stages: server ~4 d, Android ~16–19 d, iOS ~6 d. Rejected: wipe-and-download per switch, one database with a filter on 667 queries (20 of 35 DAOs lack it), renaming the old file, a proof to switch back to a held guest, "Account jorein" running 0053's move, releasing the ready fixes before the switcher | approved; being built on app `VC_108_VN_149` for **1.4.9** (1.4.8 became the owner's footer hotfix); passes to Keystore/Keychain block that release |
