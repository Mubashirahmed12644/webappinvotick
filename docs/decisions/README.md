# Decision log (ADRs)

One file per decision. **Read this index before proposing a plan** — the most common wrong turn is
re-proposing an option that was already rejected.

## Format

```markdown
# NNNN — <short title>
- **Date:** YYYY-MM-DD
- **Status:** decided | superseded by NNNN | reversed
- **Decision:** one sentence, imperative.
- **Why:** the reason in the user's terms.
- **Rejected:** the options we did NOT take, and why.
- **Consequences:** what this forces us to do / stop doing.
```

Numbering: next free number, zero-padded to 4.

## Index

| # | Decision | Status |
|---|---|---|
| [0001](0001-one-html-render-engine.md) | One HTML render engine everywhere; retire native Canvas | decided |
| [0002](0002-presentation-json-per-invoice.md) | Invoice presentation = one JSON per invoice, asset refs | decided |
| [0003](0003-currency-stored-on-invoice.md) | Store currency on the invoice row; client-locked | decided |
| [0004](0004-share-link-is-html-not-image.md) | Share link renders HTML from snapshot, not an uploaded image | decided |
| [0005](0005-edit-mints-new-share-token.md) | Editing a shared invoice mints a new token and revokes the old | decided |
| [0006](0006-g1-real-invoice-metric.md) | G1 "real invoice" = confirmed share or payment; repeat use as support | decided |
| [0007](0007-first-run-currency-ladder.md) | First currency: IP → device region → unambiguous language → USD | decided |
| [0008](0008-adaptive-layout-roadmap.md) | Adaptive layout in 5 phases, applied directly; RTL ships separately | decided |
| [0009](0009-currency-backfill-measured.md) | 2,717 of 5,004 invoices still carry the wrong currency — the backfill's WHERE clause could not see them | measured |
| [0010](0010-health-centre-one-dashboard.md) | Everything that fails silently gets a check in one place; a new check is a `@Component`, never a new page | accepted |
| [0011](0011-discard-dialog-only-for-documents.md) | Small entity forms lose the discard dialog and keep a draft; invoice/estimate keep it — the six copies are two different interactions | accepted |
| [0012](0012-no-continue-editing-dialog-on-entry.md) | A draft is resumed, not negotiated — no "continue editing?" dialog on entry | accepted |
| [0013](0013-logout-clears-session-and-purges-when-settled.md) | Logout clears the session; the account's data is deleted only once it is settled | accepted |
| [0014](0014-discard-dialog-needs-document-content.md) | A business and a client are not work worth a dialog | accepted |
| [0015](0015-estimate-draft-rules-copied-only-where-safe.md) | Estimate draft rules follow the invoice's — but only four of five | accepted |
| [0016](0016-one-bottom-sheet-standard.md) | One bottom sheet, and it answers when you pull it | accepted, shipped |
| [0017](0017-shared-pdf-android-installs-others-download.md) | On the shared link, Android installs the app for a PDF; everyone else downloads | accepted, built |
| [0018](0018-the-shared-image-is-gone-html-is-the-only-render.md) | The shared invoice image is gone; HTML is the only render | accepted, built |
| [0019](0019-two-tags-money-and-delivery-are-not-one-axis.md) | An invoice card carries two tags — money and delivery are not one axis | superseded by 0020 |
| [0020](0020-the-payment-tag-goes-the-card-says-it-in-words.md) | The payment tag goes; the card says it in words | accepted, built |
| [0021](0021-dynamic-colour-is-offered-never-assumed.md) | Dynamic colour is offered, never assumed; it stops at the document | accepted, built |
| [0022](0022-edge-to-edge-verified-for-targetsdk-36.md) | Edge-to-edge verified for targetSdk 36 — 17 surfaces measured, no code needed |
| [0023](0023-one-dismissal-event-the-method-is-a-parameter.md) | One dismissal, one event; the route (`method`) is a parameter on the sheet's own close id | decided, implemented |
| [0024](0024-a-double-tap-is-stopped-at-the-button-not-counted-later.md) | A double tap is stopped at the button, not counted around later | decided |
| [0025](0025-voice-input-is-offered-only-where-the-device-can-do-it.md) | Voice input is offered only where the device can do it | decided |
| [0026](0026-a-session-is-renewed-before-it-dies-never-revived-after.md) | A session is renewed before it dies, never revived after | decided, implemented |
| [0027](0027-a-first-time-user-is-a-device.md) | A first-time user is a device, and the journey is counted per device | decided, implemented |
| [0028](0028-the-invotick-id-is-part-of-the-account.md) | The Invotick ID is minted with the account, not on demand (reverses 2026-07-09 lazy) | decided, implemented |
| [0029](0029-a-sync-failure-is-an-attempt-the-server-refused.md) | A reported sync failure is an attempt the server refused | decided, in app branch |
| [0030](0030-every-device-that-stopped-has-a-reason.md) | Every device that stopped on the journey has exactly one reason | decided, implemented |
| [0031](0031-the-splash-shows-the-app-while-the-gate-holds.md) | The splash shows the app taking shape while the ad gate holds; gate timing untouched | remedy reversed by 0038 (diagnosis stands) |
| [0032](0032-dismissing-the-save-gate-keeps-the-invoice-as-a-draft.md) | Dismissing the Save gate keeps the invoice as a draft | decided, in app branch |
| [0033](0033-no-banner-under-the-first-open-onboarding-overlay.md) | No banner under the first-open onboarding overlay (owner's call; measure impressions alongside) | decided, in app branch |
| [0034](0034-a-page-costs-what-it-shows.md) | A page costs what it shows, not what the database holds | decided, being applied |
| [0035](0035-a-build-must-not-be-able-to-take-the-database.md) | A build must not be able to take the database | partly done, one number still to measure |
| [0036](0036-a-delete-must-say-what-it-is-deleting.md) | A delete must say what it is deleting (sync contract gap G2) | decided, not yet built |
| [0037](0037-the-ui-layer-owns-the-press.md) | The UI layer owns the press; the coded `trackClick` twin goes | decided, in app branch |
| [0038](0038-the-hold-shows-one-branded-loader-not-a-picture-of-the-screen.md) | The hold shows one branded loader, not a picture of the screen it is heading to (reverses 0031's remedy) | decided, in app branch |
| [0039](0039-language-is-read-from-the-session-not-re-sent-per-event.md) | Language is read from the session, not re-sent per event; the session row is written from any batch | decided, in backend tree |
| [0040](0040-the-system-splash-draws-the-mark-we-are-about-to-draw.md) | The system splash draws the mark we are about to draw; we cannot paint sooner than the platform, only paint the same thing | decided, in app branch |
| [0041](0041-premium-is-a-date-on-the-device-refreshed-daily.md) | Premium is a date on the device, not a flag, bounded by both the plan's expiry and a 24h verification window; the server is asked daily after the splash, and silence never revokes | **planned, awaiting approval** |
| [0042](0042-the-server-owns-the-version-and-the-device-merges.md) | The server owns the version and the device merges without asking: six push answers, delete carries and bumps its version, derived money fields are recomputed not merged (extends 0036, which has no row for the case that will actually arrive) | **decided, being built** |
| [0043](0043-an-ad-failure-is-named-in-the-sdks-own-words.md) | An ad failure is named in the SDK's own words, on the event that already exists: `reason` beside the untouched `code` because the load and show tables overlap, plus `error_domain`, one cause level, the missing `ad_request_id`, and `path` because `placement` is constant | decided, in app branch |
| [0044](0044-an-install-belongs-to-a-tag-not-to-a-link.md) | An install belongs to a tag, not to a link: the UTM tab joins the registry to `install_referrer` by exact equality on all three UTM values, a zero prints as `0`, and traffic matching no link of ours is listed uncredited | decided, built |
| [0045](0045-the-share-link-page-reports-its-own-journey.md) | The share-link page reports its own journey: six names into `/v2/analytics/track` as `platform=Web`, the app's own names where the action is the app's, `viewer_platform` not `platform`, the version floor exempted for Web, and no App Store link because there is no App Store app | decided, built, deployed 2026-09-09 |
| [0046](0046-a-sent-link-retires-only-when-its-replacement-is-sent.md) | A sent share link retires only when its replacement is confirmed sent: `create()` never revokes a sent token, `markShared()` does; an unsent token is still replaced at once; snapshots compare as content, with embedded images masked | decided, built |
| [0047](0047-plays-answer-decides-premium-on-a-device.md) | Play's answer decides premium on a device, guest or signed in: ads stop the moment Play reports a purchase, another account's purchase still makes this phone premium, "not premium" needs Play's confirmation before any ad is shown, requests never wait, and the splash waits at most 1.5 s from its start | decided, built (next release 1.4.5) |
| [0048](0048-the-paywall-says-what-google-charges.md) | What the paywall says is what Google charges — price, period and saving from the offer actually bought, never a product's name; a refusal is final only when Google gives it (400), "could not ask" is 503; the server keeps tokens and every restore answer; an admin can defer one customer's renewal | decided, built (next release 1.4.5) |
| [0049](0049-the-app-open-ad-is-asked-for-at-the-first-onstart.md) | The app-open ad is asked for at the first onStart, not when the SDK is ready: the only question is the saved premium flag (false by default), Play answers in parallel, the show still refuses premium and still waits (≤1.5 s from splash start) for Play's "not premium"; cold-start requests move from `path=splash` to `path=preload` | decided, built, verified on the Pixel (next release 1.4.5) |
| [0050](0050-every-sync-failure-carries-its-own-evidence.md) | Every sync failure carries its own evidence, joined by one request id: `X-Request-Id` app → server → response → `sync_failure.last_trace_id` → Loki; `sync_failed` gains ids, codes and versions (never field values); the ingest keeps device, version and record and shares the server's signature; promtail stops labelling `traceId`; the panel shows the evidence and the server's log lines | decided, live (backend + panel; app in 1.4.5) |
| [0051](0051-sync-and-billing-each-have-an-owner-agent.md) | Sync and billing each have a dedicated agent (`.claude/agents/sync.md`, `billing.md`) whose file holds that domain's policy in one place; a decided rule is written there first, memory keeps incidents and dated state, and every line is checked against code | decided |
| [0052](0052-the-business-view-defaults-to-all-and-the-last-choice-is-remembered.md) | With more than one business, every business-scoped screen opens on All Businesses until the user chooses; the latest choice (All or one business) is remembered on every open, on the phone and the web, one choice for all screens; the business a new document uses is remembered separately (reverses the 2026-07-25 "never auto-select All") | decided, to build |
| [0053](0053-a-guests-work-joins-an-existing-account-only-when-the-user-says-so.md) | Sign-up moves a guest's work silently; sign-in to an existing account asks once when there is real work; the move is one server step with proof the phone held that guest (no record-by-record takeover); declined work kept 90 days; businesses never merged by name; the 26 records already moved stay | decided, to build |
| [0054](0054-the-share-card-is-rendered-on-demand-and-cached-at-the-edge.md) | The share link's card is rendered on demand and cached at the edge; the Blob path goes (production never had a token) | decided, to build |
| [0055](0055-a-legacy-invoice-without-a-client-is-closed-for-good.md) | The one legacy invoice without a client (class L) is left as it is and never raised again | decided |
| [0056](0056-an-estimate-date-is-stored-as-a-calendar-date.md) | An estimate's date is stored as a calendar date, not a noon-UTC instant (wrong at UTC+12 and beyond); migration first, then code | decided, to build |
| [0057](0057-edits-a-phone-gave-up-during-the-clock-bug-are-sent-again-once.md) | Edits a phone marked TERMINAL during the clock bug are sent again once with the phone's copy, only where no other device changed the record since | decided, to build |
| [0162](0162-no-ad-loads-before-consent-and-the-form-comes-after-the-user-has-landed.md) | The app had no GDPR consent at all: Google UMP now gates every ad load, the form is never shown on the splash but at the first ad-moment after the user has landed, the cold-start app-open ad it costs is counted as `consent_pending`, and two coded first-party events trace the funnel | decided, built (Android, app branch) |
| [0163](0163-the-free-invoice-tool-reports-its-own-funnel-and-offers-the-app-after-the-pdf.md) | The free invoice tool at `/` (NOT `/free-invoice` — that route has never existed) had **zero events**, so every number about it was a zero that meant "nobody asked". It gets seven coded web events through the existing route, all `surface=web_free_tool`: `free_invoice_page_view`, `_form_typed` (`form`, once per form per visit — the app's typing proxy, one name instead of three), `_completed` (`source`, `items`, `has_logo`, `has_tax`, `has_discount`, `optional_fields` — the app's parameters lost in 1.4.3, put back), `_pdf_download` (`outcome`, `source`, `exception_class` — the value moment and the G1 signal for this surface), and the offer's `_install_offer_shown` / `_click` (`play_store\|app_store\|web_account`) / `_dismissed` (`close_button\|not_now`). `source` = `typed\|sample\|sample_edited` answers the G1 question this surface has and the app does not: "✨ Surprise me" can reach a PDF with nothing typed. Owner's option A: a **soft** offer after the PDF is produced, never blocking anything, once per visit, Android → Play and iPhone/desktop → a free account because there is no App Store listing (`itunes.apple.com/lookup` = 0 results, 2026-09-23). The copy states both limits rather than hiding them: a web invoice does **not** follow the user into the app, and the browser is not storage. Rejected: gating anything behind the install (option B), cloning the app's screens including its splash (a known ~20 % drop-off the owner is reducing), a second analytics channel, reusing the app's event names, a `pdf_saved` name that claims an unobserved outcome, and a `localStorage` once-mark. Measure first, A/B after — `trigger` is a parameter for that reason. | built, `feat/free-invoice-funnel-and-install-offer` off `gitlab/main` `3a9eb10`; 24 events verified end to end against a local capture server, 22 unit tests, tsc + eslint clean; **not deployed** |
| [0169](0169-the-first-screen-one-account-word-no-dead-button-and-the-invoice-inside-the-fold.md) | The landing's **first screen**, fixed against Invoice Fly's side by side. (1) **One account word, not two** — the header carried *Sign in* AND *Create account* above a promise reading "No account, no watermark, nothing to install", so the screen argued with itself before a sentence was read; theirs carries exactly one ("Log in"). `/signup` is untouched and the account offer still comes after the PDF (0163). (2) **The dead "App Store — SOON" badge is gone** — a control that cannot be pressed spends belief in every other button; it is now one quiet line in the footer, keeping its `order` so **0164's device-aware ordering is intact** and the real badge drops into that position the day `APP_STORE_URL` exists. (3) **Our two true facts are on the screen**: `4,000+ businesses · 96% never made an account` — the second is *proof of* the promise above it, not a claim about it. Their 4.8 stars / 125,000 / NYT·Forbes·TechCrunch are earned and **not copyable: Invotick has no Play rating at all**, so no number was invented. (4) **The store badges moved to the footer — from THEIR page, not from judgement**: the owner's `40-first-screen-scrolled-footer.png` shows both badges at the very bottom after Pricing/Blog/Help Center, with no store button above the fold; ours ran **331→505 px between the primary button and the invoice**. Measured result: **the sample invoice moved 505→807 (grazing the edge) → 367→669, fully inside the fold** — and their first screen shows **no product at all**, which is our single biggest advantage on this screen. (5) **The stale-year bug, nearly ours too**: their footer says "© 2024" on a page served today; ours was never hardcoded but `/` is statically prerendered, so `getFullYear()` froze at **build** time — `FooterYear` now reads it in the browser. Left alone deliberately: the sample invoice, the single filled "Create invoice", the Play badge's behaviour, and the FAQ (measured at 820, after the invoice, pushing nothing down). **Where they are still better, plainly: trust** — stars, a user count and three mastheads that two sentences cannot replace. Found and NOT fixed, out of scope: **the public landing has no dark theme** — `prefers-color-scheme: dark` still renders light. | **deployed** — `feat/first-screen` off `main` `de7449c`; 44/44 tests, `tsc` clean, eslint at baseline, `next build` keeps `○ /`; re-measured live at 375×812 and at a 1.5× font, flow behind the screen re-run end to end |
| [0168](0168-the-onboarding-invoice-flys-shape-our-facts-and-what-it-costs-in-taps.md) | `/` gains the onboarding the owner approved, built from Invoice Fly's own screens: **three value slides** (black, our own invoice as the artwork, two-line headline, full-width Continue, **no skip/dots/counter**, a quiet *Sign in* top-right on slide 1 only) → a "One moment…" that only appears if the local read is slow → **four questions** (business 25%, trade 50%, logo 75%, the logo we made 100%) → **"You're ready"** (never "Account created!", because we create none) → the empty invoices home → the first invoice with a **coach-mark** on the client block. **The cost, measured and not smoothed over: 6 taps → 15** from landing to a downloaded invoice (screens 6 → 14; machine time 2.3 s → 6.1 s; modelled human time ~16 s → ~35 s, and the time is a MODEL, only the taps are measured). That is materially worse for G1 and the owner asked to see it live in order to judge it; their own onboarding takes ~2 minutes behind a sign-in wall, so we are still far ahead, but the six-tap flow was what made this page different. **No escape hatch was added** — it would blunt the very number he asked for, and the approved design has none. The generated logo is drawn **on the phone from the typed name**, in **8 designs**, with the trade's glyph only when it really renders: their AI printed "Touchpenty" for "Touchpedia" on the mark that goes on the first invoice, and a wrong name on an invoice is a G3 cost. **The "129 industry icons" the design cites do not exist** — searched across both repos and the memory — so nothing claims them; instead each of 37 trades carries a glyph and one of the **ten real templates** we do ship, so the question changes the paper. **Analytics: no new event name.** The whole flow is values on `free_invoice_step_reached.step` (`slide_2 … home`), plus `regenerated` on `free_invoice_logo_choice` and `industry` on `free_invoice_completed`; `slide_1` and the loading screen deliberately have none, and **`business` is now a value where 0165 refused it** — split any funnel spanning this deploy by date. A hole found by measuring: the hand-over opened the invoice screen at the client step without firing `step=client`, so that step would have read zero while everybody reached it. Not built, and named so each is a choice: **no PRO crown, no six-tab bar** (controls with no web destination — a crown that opens nothing is an invented gate), **no paywall**, **no ad and no space for one**. **Corrects 0166**: its claim that `LandingExperience.reveal()` used `flushSync` was false — a script asserted after mutating and wrote only at the end, so the doc landed and the code did not, leaving `hashOpen` testing `=== "#create"` while the comment said `startsWith`; a reload on `#create-client` rendered the hero. Both fixed here. | **deployed** — `feat/onboarding` off `main` `2b84d78`; 44/44 tests (10 new, the schema-drift test calibrated red), `tsc` clean, eslint at the pre-existing baseline, `next build` keeps `○ /`; stress-swept at 320×667 and 375×400, 1× and 1.5×, LTR and RTL, every step and Back through all of them, detector calibrated |
| [0166](0166-the-keyboard-carries-the-guided-invoice-and-back-means-one-step-back.md) | The guided flow (0165) is finished to its intended shape: **the keyboard carries the reader between steps**. Four defects measured on the live page and closed. (1) Focus did not carry — `activeElement` was **`BODY`** after Enter on step 1 and again on step 2, so the keyboard dropped and three of the journey's six taps existed only for that; the step change is now `flushSync`ed and the focus taken **inside the same gesture handler**, because iOS Safari keeps a keyboard up only for a focus in the gesture's own call stack (`LandingExperience.reveal()` moved to the same pattern). (2) The item step had **`enterKeyHint: null`** on all three fields; it now has two fields — description `next`, price `go` — with quantity on a `× 1` chip (**Q1 option A**), because quantity is 1 on nearly every first invoice and an iPhone decimal pad has **no return key at all**, so having one number field instead of two is the difference between one tap at the end and two. (3) At 375×400 the sticky bar sat **327–400** and took `+ Add item`, Subtotal and Total behind it; **the total now rides in the bar**, the subtotal is drawn only when it differs, every field carries `scroll-margin-bottom: 128px` so the browser's focus-scroll parks it **228–272** against a bar at **291–400**, and a `@media (max-height:620px)` spacer gives the last element room to clear. (4) `history.length` was **2 on step 1 and still 2 on step 4** — the flow created no history; each step now pushes one (`#create` → `#create-client` → `#create-items` → `#create-done`), so Back walks it backwards and leaves the page only from step 1. **Q2 option A**: the generated logo is kept quietly with one *Change* button instead of Keep·Change·Skip — so `free_invoice_logo_choice` stops sending `kept` and `skipped` (no press left to report), and what they saw moves onto **`free_invoice_completed`'s new `logo_source` = `generated\|uploaded`**, a parameter on a surviving event, never a new name. `free_invoice_step_reached`'s first-time guard became a set, because with Back the old `next > step` test would have double-counted a step. **Q3 is settled by the owner: no ads on the web tool, ever** — we have no AdSense approval — so nothing here reserves a slot or compromises a layout for one. Honest limit: **2 taps on Android/desktop, 3 on iPhone**, because no `enterkeyhint` can put a return key on a decimal pad. Rejected: focusing from a `useEffect`; auto-advancing when both fields are non-empty; the total inside the button's label (the currency control would lose its home — invariant 6); a thinner bar; unconditional panel padding; a `kept` fired with no press; keeping the URL on a reload. | **deployed** — `feat/guided-two-taps` off `main` `cd17ebc`; 34/34 tests (2 new, calibrated red without the fix), `tsc` clean, no new eslint errors, `next build` keeps `○ /`; stress-swept at 375×812 / 375×400 / 320×667, 1× and 1.5× font, LTR and RTL, all four steps: no side scroll and no money outside its box, with the detector calibrated by displacing a figure on purpose |
| [0165](0165-the-landing-asks-three-questions-and-the-tool-reports-who-pressed-the-button.md) | `/` stops being one long form: pressing "Create invoice" opens a **guided three-step flow** — business → client → items, the app's own three questions (`docs/android-first-invoice-flow.md` §1) — then the finished invoice with Download PDF. The full editor is still there, one press away at the end, over **the same draft**: one `useFreeInvoice()`, two renderers, because a second copy of the state would have meant two autosaves over one IndexedDB row and the funnel events firing twice. The editor stays mounted-hidden, which keeps its markup in the static HTML **and** keeps `#fi-paper` in the document, so step 4's download works with the editor never on screen. Four defects measured on the live page and fixed: the CTA's bottom edge sat at **696 px against a 667 px viewport** (now 311 px, and 596 px at a 1.5× font); "Create account" was an outlined button in the best corner of a page promising no sign-up (now a text link — one filled element per screen); the same "free / no sign-up" promise appeared three times (now once); and the sample invoice was `$1,200.00` billed to Leeds while the traffic is India / South Africa / Somalia / Zambia / Zimbabwe / Nigeria / Sri Lanka (now the visitor's own currency, and no address at all). The app's silent generated logo becomes visible as **Keep · Change · Skip**, and keeping it makes `has_logo=true` honestly true. Four new events: **`free_invoice_tool_opened`** (`method` = `cta_press\|deep_link` — 0164 specified it and never built it, so nothing had ever counted the press this page exists to cause), **`free_invoice_step_reached`** (`step` = `client\|items\|done`; `business` is deliberately not a value, because reaching step 1 IS the tool opening and two names for one moment is one press counted twice), **`free_invoice_logo_choice`** (`kept\|change_opened\|skipped` — `change_opened` is the picker opening and nothing more), **`free_invoice_store_badge_click`** (`play_store\|app_store`, a separate name from the install offer's click because they are two populations). Stress-tested at 375×667, 375×812 and 320 px, at 1.5× font, in RTL, with long names and `₨1,284,500.75`: found and fixed the line amount being **painted 36 px outside its own pill** at 320 px/1.5× (money drawn over the price it was computed from — G3), the same defect 4 px wide and pre-existing in the full editor, a duplicate `fi-business-name` id across the two views, and the editor being squeezed to 560 px by the steps' reading width. Rejected: a `/create` route, a second copy of the state, unmounting the editor, a name per step, a `changed` value that would claim an unobserved outcome, hiding the glimpse on phones, and re-scaling the sample's amounts per currency. | built, `feat/guided-first-invoice` off `gitlab/main` `5b5550f`; 11 new schema tests + 5 logo tests (32/32 pass), every event verified end to end by its stored shape against a capture server, `tsc` + eslint clean, `next build` keeps `○ /`; **not deployed** |
| [0170](0170-the-business-s-own-footer-is-premium-and-every-screen-says-so.md) | **The business's own footer (0151) is Premium**, and every screen that describes it now says the same thing. The owner, 2026-09-25: *"apna footer premium main mily ga, aur is per jhoot bilkul nhi hona chahye — har jagha sach ho"*, widened to *"jo aik device per hy wo sab device per ho"*. **The gate was already built and is already per account**, not per install — checked, not assumed: `AppViewModel:437-442` → `ObservePremiumUseCase` → `shownPremium` (the store's word on this phone **or** the account's dated grant), stamped into every document by the two factories (0147), and read by every renderer (`SharedComponents.kt:676-698`, `PdfGenerator.android.kt:455-478`, `footerMode()` in `src/lib/invotick-footer.ts:75-80`); the share link asks the server about the **account** (`SharedInvoiceService.kt:612-613` → `PremiumUserIds`), **proven live in production** — `GET /v2/shared-invoice/{token}` returns `showInvotickFooter`. So nothing needed gating; what needed fixing was what we told people. **Three false or missing sentences, each with its proof.** (1) The **paywall never mentioned the own footer at all** while premium was delivering it — the same fault that made the first customer's purchase an accident (billing practice rule 3); a third card now claims it, and the card disappears when Remote Config `own_footer_enabled` is off, because a claim false on the screen it is read is the thing the owner's rule forbids. (2) **`/terms` said "Today its main benefit is removing ads"** — false since 2026-09-21, when 1.4.8 (vc107, **83,826 events in 3 days** = the build most phones are on) and the live server also took the footer off, old links included; rewritten, with the own-footer clause deliberately **held back until 1.4.9 is released**. (3) **The landing promised "no watermark"** while the free tool's PDF carries the full Invotick band — the PDF is a bitmap of that very preview (`lib/free-invoice/pdf.ts:7-27`) and `toRenderData` never sets `hideInvotickFooter`; rendered with `react-dom/server`, the markup contains "Invoice generated using Invotick", the `gw.invotick.com` link and the "Scan to download" QR (`scripts/checks/free-tool-footer.check.tsx`, 4/4). The footer **stays** on the free tool — it is the growth surface (G2); only the sentence changed. **The share-link question, stated rather than reopened**: frozen-only keeps our branding on a payer's old links for ever, live-only puts branding back onto a sent invoice under the client's eyes; what is built and is kept is the hybrid — the Invotick footer comes off **live by the account**, the own footer's **content is frozen** in the snapshot, and `withOwnersFooterRule` can only ever remove, never restore. **Could not check: the Play and App Store listings** — not in any repo. **Found in passing:** of 1,312 share snapshots in 30 days, 3 carry `ownFooter` and 2 of those are **free** accounts, one with a `contactLine` — a business's phone and email riding inside a public link's JSON without being drawn. | **NOT deployed, no release built** — web `fix/own-footer-premium-claims` off `gitlab/main` (`next build` green, 44/44 tests, premium-footer 8/8, new free-tool-footer 4/4, `tsc` clean); app `fix/paywall-claims-the-own-footer` off `VC_113_VN_149`, **not compiled** (an iOS archive was running, so Gradle was not started) |
- [0171 — A coaching tooltip draws nothing while anything covers the screens](0171-a-coaching-tooltip-draws-nothing-while-anything-covers-the-screens.md)
- [0172 — The sheet back-stack crash is already fixed, and the second fix is refused](0172-the-sheet-back-stack-crash-is-already-fixed-and-the-second-fix-is-refused.md)
- [0173 — The iPhone asks Google's consent question before Apple's](0173-the-iphone-asks-googles-consent-question-before-apples.md)
- [0175 — The share page reads its labels from the table, and only the invoice's own sentences are translated live](0175-the-share-page-reads-its-labels-from-the-table.md)
