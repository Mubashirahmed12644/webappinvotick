# Decision log (ADRs)

One file per decision. **Read this index before proposing a plan** — the most common wrong turn is
re-proposing an option that was already rejected.

## Format

```markdown
# NNNN — <short title>
- **Date:** YYYY-MM-DD
- **Status:** decided | superseded by NNNN | reversed
- **Decision:** one sentence, imperative.
- **Why:** the reason in the user's terms.
- **Rejected:** the options we did NOT take, and why.
- **Consequences:** what this forces us to do / stop doing.
```

Numbering: next free number, zero-padded to 4.

## Index

| # | Decision | Status |
|---|---|---|
| [0001](0001-one-html-render-engine.md) | One HTML render engine everywhere; retire native Canvas | decided |
| [0002](0002-presentation-json-per-invoice.md) | Invoice presentation = one JSON per invoice, asset refs | decided |
| [0003](0003-currency-stored-on-invoice.md) | Store currency on the invoice row; client-locked | decided |
| [0004](0004-share-link-is-html-not-image.md) | Share link renders HTML from snapshot, not an uploaded image | decided |
| [0005](0005-edit-mints-new-share-token.md) | Editing a shared invoice mints a new token and revokes the old | decided |
| [0006](0006-g1-real-invoice-metric.md) | G1 "real invoice" = confirmed share or payment; repeat use as support | decided |
| [0007](0007-first-run-currency-ladder.md) | First currency: IP → device region → unambiguous language → USD | decided |
| [0008](0008-adaptive-layout-roadmap.md) | Adaptive layout in 5 phases, applied directly; RTL ships separately | decided |
| [0009](0009-currency-backfill-measured.md) | 2,717 of 5,004 invoices still carry the wrong currency — the backfill's WHERE clause could not see them | measured |
| [0010](0010-health-centre-one-dashboard.md) | Everything that fails silently gets a check in one place; a new check is a `@Component`, never a new page | accepted |
| [0011](0011-discard-dialog-only-for-documents.md) | Small entity forms lose the discard dialog and keep a draft; invoice/estimate keep it — the six copies are two different interactions | accepted |
| [0012](0012-no-continue-editing-dialog-on-entry.md) | A draft is resumed, not negotiated — no "continue editing?" dialog on entry | accepted |
| [0013](0013-logout-clears-session-and-purges-when-settled.md) | Logout clears the session; the account's data is deleted only once it is settled | accepted |
| [0014](0014-discard-dialog-needs-document-content.md) | A business and a client are not work worth a dialog | accepted |
| [0015](0015-estimate-draft-rules-copied-only-where-safe.md) | Estimate draft rules follow the invoice's — but only four of five | accepted |
| [0016](0016-one-bottom-sheet-standard.md) | One bottom sheet, and it answers when you pull it | accepted, shipped |
| [0017](0017-shared-pdf-android-installs-others-download.md) | On the shared link, Android installs the app for a PDF; everyone else downloads | accepted, built |
| [0018](0018-the-shared-image-is-gone-html-is-the-only-render.md) | The shared invoice image is gone; HTML is the only render | accepted, built |
| [0019](0019-two-tags-money-and-delivery-are-not-one-axis.md) | An invoice card carries two tags — money and delivery are not one axis | superseded by 0020 |
| [0020](0020-the-payment-tag-goes-the-card-says-it-in-words.md) | The payment tag goes; the card says it in words | accepted, built |
| [0021](0021-dynamic-colour-is-offered-never-assumed.md) | Dynamic colour is offered, never assumed; it stops at the document | accepted, built |
| [0022](0022-edge-to-edge-verified-for-targetsdk-36.md) | Edge-to-edge verified for targetSdk 36 — 17 surfaces measured, no code needed |
| [0023](0023-one-dismissal-event-the-method-is-a-parameter.md) | One dismissal, one event; the route (`method`) is a parameter on the sheet's own close id | decided, implemented |
| [0024](0024-a-double-tap-is-stopped-at-the-button-not-counted-later.md) | A double tap is stopped at the button, not counted around later | decided |
| [0025](0025-voice-input-is-offered-only-where-the-device-can-do-it.md) | Voice input is offered only where the device can do it | decided |
| [0026](0026-a-session-is-renewed-before-it-dies-never-revived-after.md) | A session is renewed before it dies, never revived after | decided, implemented |
| [0027](0027-a-first-time-user-is-a-device.md) | A first-time user is a device, and the journey is counted per device | decided, implemented |
| [0028](0028-the-invotick-id-is-part-of-the-account.md) | The Invotick ID is minted with the account, not on demand (reverses 2026-07-09 lazy) | decided, implemented |
| [0029](0029-a-sync-failure-is-an-attempt-the-server-refused.md) | A reported sync failure is an attempt the server refused | decided, in app branch |
| [0030](0030-every-device-that-stopped-has-a-reason.md) | Every device that stopped on the journey has exactly one reason | decided, implemented |
| [0031](0031-the-splash-shows-the-app-while-the-gate-holds.md) | The splash shows the app taking shape while the ad gate holds; gate timing untouched | remedy reversed by 0038 (diagnosis stands) |
| [0032](0032-dismissing-the-save-gate-keeps-the-invoice-as-a-draft.md) | Dismissing the Save gate keeps the invoice as a draft | decided, in app branch |
| [0033](0033-no-banner-under-the-first-open-onboarding-overlay.md) | No banner under the first-open onboarding overlay (owner's call; measure impressions alongside) | decided, in app branch |
| [0034](0034-a-page-costs-what-it-shows.md) | A page costs what it shows, not what the database holds | decided, being applied |
| [0035](0035-a-build-must-not-be-able-to-take-the-database.md) | A build must not be able to take the database | partly done, one number still to measure |
| [0036](0036-a-delete-must-say-what-it-is-deleting.md) | A delete must say what it is deleting (sync contract gap G2) | decided, not yet built |
| [0037](0037-the-ui-layer-owns-the-press.md) | The UI layer owns the press; the coded `trackClick` twin goes | decided, in app branch |
| [0038](0038-the-hold-shows-one-branded-loader-not-a-picture-of-the-screen.md) | The hold shows one branded loader, not a picture of the screen it is heading to (reverses 0031's remedy) | decided, in app branch |
| [0039](0039-language-is-read-from-the-session-not-re-sent-per-event.md) | Language is read from the session, not re-sent per event; the session row is written from any batch | decided, in backend tree |
| [0040](0040-the-system-splash-draws-the-mark-we-are-about-to-draw.md) | The system splash draws the mark we are about to draw; we cannot paint sooner than the platform, only paint the same thing | decided, in app branch |
| [0041](0041-premium-is-a-date-on-the-device-refreshed-daily.md) | An account's premium reaches every device of it: Play's flag, or else the server's confirmed grant, held until the server's `offlineValidUntil` — a day inside the store's refund window (Play 48 h, Apple 14 days), the plan's date after it (the owner's design; a build with no window was rejected). Asked once a day after the splash, at once on a sign-in or device-link; the server checks Google each time, at most once per purchase in 10 min. CANCELLED with time left is premium, Google unreachable is never a no. Rejected: one flag for both, asking in the splash, a new route, no window | **approved by the owner 2026-09-14; built for 1.4.6** (backend and app branches `…every-device`); not deployed |
| [0042](0042-the-server-owns-the-version-and-the-device-merges.md) | The server owns the version and the device merges without asking: six push answers, delete carries and bumps its version, derived money fields are recomputed not merged (extends 0036, which has no row for the case that will actually arrive) | **decided, being built** |
| [0043](0043-an-ad-failure-is-named-in-the-sdks-own-words.md) | An ad failure is named in the SDK's own words, on the event that already exists: `reason` beside the untouched `code` because the load and show tables overlap, plus `error_domain`, one cause level, the missing `ad_request_id`, and `path` because `placement` is constant | decided, in app branch |
| [0044](0044-an-install-belongs-to-a-tag-not-to-a-link.md) | An install belongs to a tag, not to a link: the UTM tab joins the registry to `install_referrer` by exact equality on all three UTM values, a zero prints as `0`, and traffic matching no link of ours is listed uncredited | decided, built |
| [0045](0045-the-share-link-page-reports-its-own-journey.md) | The share-link page reports its own journey: six names into `/v2/analytics/track` as `platform=Web`, the app's own names where the action is the app's, `viewer_platform` not `platform`, the version floor exempted for Web, and no App Store link because there is no App Store app | decided, built, deployed 2026-09-09 |
| [0046](0046-a-sent-link-retires-only-when-its-replacement-is-sent.md) | A sent share link retires only when its replacement is confirmed sent: `create()` never revokes a sent token, `markShared()` does; an unsent token is still replaced at once; snapshots compare as content, with embedded images masked | decided, built |
| [0047](0047-plays-answer-decides-premium-on-a-device.md) | Play's answer decides premium on a device, guest or signed in: ads stop the moment Play reports a purchase, another account's purchase still makes this phone premium, "not premium" needs Play's confirmation before any ad is shown, requests never wait, and the splash waits at most 1.5 s from its start | decided, built (next release 1.4.5) |
| [0048](0048-the-paywall-says-what-google-charges.md) | What the paywall says is what Google charges — price, period and saving from the offer actually bought, never a product's name; a refusal is final only when Google gives it (400), "could not ask" is 503; the server keeps tokens and every restore answer; an admin can defer one customer's renewal | decided, built (next release 1.4.5) |
| [0049](0049-the-app-open-ad-is-asked-for-at-the-first-onstart.md) | The app-open ad is asked for at the first onStart, not when the SDK is ready: the only question is the saved premium flag (false by default), Play answers in parallel, the show still refuses premium and still waits (≤1.5 s from splash start) for Play's "not premium"; cold-start requests move from `path=splash` to `path=preload` | decided, built, verified on the Pixel (next release 1.4.5) |
| [0050](0050-every-sync-failure-carries-its-own-evidence.md) | Every sync failure carries its own evidence, joined by one request id: `X-Request-Id` app → server → response → `sync_failure.last_trace_id` → Loki; `sync_failed` gains ids, codes and versions (never field values); the ingest keeps device, version and record and shares the server's signature; promtail stops labelling `traceId`; the panel shows the evidence and the server's log lines | decided, live (backend + panel; app in 1.4.5) |
| [0051](0051-sync-and-billing-each-have-an-owner-agent.md) | Sync and billing each have a dedicated agent (`.claude/agents/sync.md`, `billing.md`) whose file holds that domain's policy in one place; a decided rule is written there first, memory keeps incidents and dated state, and every line is checked against code | decided |
| [0052](0052-the-business-view-defaults-to-all-and-the-last-choice-is-remembered.md) | With more than one business, every business-scoped screen opens on All Businesses until the user chooses; the latest choice (All or one business) is remembered on every open, on the phone and the web, one choice for all screens; the business a new document uses is remembered separately (reverses the 2026-07-25 "never auto-select All") | decided, to build |
| [0053](0053-a-guests-work-joins-an-existing-account-only-when-the-user-says-so.md) | Sign-up moves a guest's work silently; sign-in to an existing account asks once when there is real work; the move is one server step with proof the phone held that guest (no record-by-record takeover); declined work kept 90 days; businesses never merged by name; the 26 records already moved stay | decided, to build |
| [0054](0054-the-share-card-is-rendered-on-demand-and-cached-at-the-edge.md) | The share link's card is rendered on demand and cached at the edge; the Blob path goes (production never had a token) | decided, to build |
| [0055](0055-a-legacy-invoice-without-a-client-is-closed-for-good.md) | The one legacy invoice without a client (class L) is left as it is and never raised again | decided |
| [0056](0056-an-estimate-date-is-stored-as-a-calendar-date.md) | An estimate's date is stored as a calendar date, not a noon-UTC instant (wrong at UTC+12 and beyond); migration first, then code | LIVE 2026-09-13 23:29 UTC: the migration in batch10 (`cd35e52`), the code and the switch at 93 in batch11 (`751d17b`); the three examples read 2026-08-11 |
| [0057](0057-edits-a-phone-gave-up-during-the-clock-bug-are-sent-again-once.md) | Edits a phone marked TERMINAL during the clock bug are sent again once with the phone's copy, only where no other device changed the record since | decided, to build |
| [0058](0058-when-two-devices-changed-the-same-field-the-later-edit-wins-by-a-corrected-clock.md) | When two devices changed the same field, the later edit wins, not the later arrival; the edit time is HLC-style (corrected to the server's clock, never backwards, after what the phone had seen, never in the future); the version still decides whether an edit may apply; a full HLC is rejected (amends 0042) | decided, to build with the receipt number |
| [0059](0059-a-delete-of-a-record-the-server-does-not-hold-is-not-a-sync-failure.md) | A delete answered NOT_FOUND is filed as `NOT_FOUND_ON_DELETE` (the server holds no row of it; nothing removes a synced row), both sides under one signature; the Device sync card shows it and never counts it; logged at INFO; the device is still told NOT_FOUND; an update answered NOT_FOUND stays a defect; the 1.4.6 app half drops the pair on the server's answer, and before sending only with a sent-marker | decided; server live since 2026-09-12 16:10 UTC (`stage` @ `587b33d`); app half A in 1.4.6 (`3cfc2d75`, merged, not released), B not built |
| [0060](0060-a-pull-never-deletes-a-row-and-a-delete-here-is-the-rows-latest-change.md) | Class P: a pull finds the local row by its id alone (deleted or not, whoever's) and never deletes it — an UPDATE for a row it holds, an ABORT insert for one it does not; a delete made on the phone is the row's latest change (contract L7); no Room schema change (nullable `productId` and a changed foreign key both rejected: they turn a loud failure into silent damage) | decided; built for 1.4.6 in `ProductSyncHandler`, not merged; the other 20 handlers open |
| [0061](0061-a-failed-app-open-show-says-who-refused-and-in-what-words.md) | A failed app-open show carries `reason` + `refused_by` (`guard` = our check in `show()`, `sdk` = the SDK's show-table word) on `app_open_decision`; no new event; the splash's `ad_show_failed` outcome keeps its name | decided, built for 1.4.6 (not merged) |
| [0062](0062-a-request-whose-screen-closed-is-not-a-failed-open.md) | A shared-invoice request whose screen closed is not a failed open: the repository lets the cancellation travel; no `reason=cancelled`; `createShareLink` untouched | decided, built for 1.4.6 (not merged) |
| [0063](0063-leaving-from-the-item-form-is-a-named-stop-on-rung-7.md) | Rung 7 names `left_on_item_form` from the screen on the device's last background (by `event_timestamp`); the same rule for rung 3's invoice screen is the owner's call | decided, built on a backend branch (not pushed) |
| [0064](0064-a-shipped-image-the-build-cannot-produce-reports-itself.md) | A shipped image the build cannot produce reports itself: new coded event `bundled_image_unavailable` (`image`, `stored_ext`, `failure` = `not_in_build` / `read_failed`, `exception_class`), once per image per process; a Health Centre card is owed | decided, built for 1.4.6 (not merged) |
| [0065](0065-a-failed-guest-sign-in-carries-its-calls-evidence.md) | A failed guest sign-in carries its call's evidence on `sync_failed stage=guest_auth` (`request_id`, `http_status`, the thrown `exception_class`); not on `guest_login_failed`, which makes no call; amends 0050's table | decided, built for 1.4.6 (not merged) |
| [0066](0066-leaving-from-the-invoice-screen-with-no-tap-recorded-is-a-named-stop-on-rung-3.md) | Rung 3 names `left_on_invoice_screen` (last background on `create_inv_scr`, no tap, back or exit recorded); the ad/premium dialog's taps are denylisted, so the label says no tap recorded, never touched nothing (0 of 81 sent the dialog's coded events); amends 0063 | built on a backend branch at the lead's direction (not pushed) |
| [0067](0067-an-applied-write-says-its-number-and-a-copy-that-changes-nothing-keeps-it.md) | The receipt number's phase 1b, server only, changing no decision: every applied write answers `data: {version, updatedAt}`; a copy that changes nothing keeps its number (judged by Hibernate's own state, its time still stored because the clock judges later copies by it); a delete may carry `{id, version}` as evidence; the all-zero id is no writer; five counters act on nothing. Rejected: the plan's projection compare, and holding the time back | built on backend branch `feat/sync-phase-1b` (`9cb19f8`), not deployed |
| [0068](0068-a-client-is-deleted-like-every-synced-record-and-a-client-in-use-is-refused.md) | A client is soft-deleted and its DELETE is queued (none of the 5 deleted clients had ever come from a phone); a client that any invoice, estimate or payment points at is refused, all or nothing, with a clear message, until the owner rules (A refuse / B hide / C cascade) | decided, built for 1.4.6; in-use rule open |
| [0069](0069-a-full-pull-sends-a-child-only-with-its-parents.md) | A full pull sends a child only with its parents (a line with its invoice or estimate, a payment link with both ends), and the deleted terms a live invoice names as deleted rows; a 1.4.6 phone asks for a full pull only after a delta's miss and waits 1 h / 6 h / 24 h after one that still fails. Accounts that would trap a new phone: 262 → 8. Rejected: deleted invoices as deleted rows, holding the cursor, a server cascade now | decided; backend `d916a6d`, app `62c06408`; repair (Q1) and root (Q2) open |
| [0070](0070-a-confirmed-grant-stays-confirmed-and-only-a-refusal-the-server-names-is-googles.md) | A grant the server confirmed is never marked unconfirmed again; a register refusal is Google's only when the server names it (`400` + `code: "NOT_VALID"`), everything else is "could not ask" and premium stands on Play's word; billing calls send `X-Device-Id`; the paywall prints no blank price and never launches a plan without an offer. Rejected: reading the message, every 400 as Google's, `code` on the shared `ApiResponse` | decided; app built for 1.4.6 (`9d8f7309`…`a56d8071`); server half being built |
| [0071](0071-a-refusal-a-service-raises-answers-the-status-it-carries.md) | A `CustomValidationError` answers the status it carries (400/401/403/404) with its own message, logged at WARN, instead of `500 An unexpected server error occurred`; no throw site, transaction rule or sync answer moves (0 production 500s from it in 15 days). Rejected: making it a `ResponseStatusException` (the JWT filter reads it as a bad token), changing throw sites, one flat 400/422 | decided; backend `15c0820`, not deployed |
| [0072](0072-the-ip-routes-answer-admins-only.md) | `/v1/ip/**` leaves `security.public-paths` and `IpRecordController` is `@RequireRole(ADMIN)`: anybody could list IP records with locations, delete them and spend the paid lookup quota with no token (0 requests in 15 days; the panel's one caller sends an admin token). Rejected: leaving it public, and only taking it off the list (guests would still get in) | decided; backend `fba532c`, live 2026-09-13 00:26 UTC (batch3) |
| [0073](0073-four-more-public-routes-stop-answering-strangers.md) | Four more public routes stop answering strangers: `push-health` deleted (Firebase ids, no caller), springdoc off except `local` (39 → 36 public paths), `refCode` URL-encoded, the phone backfill ADMIN-only with `limit` ≤ 5,000; `admin-login-as-user` (admin password alone → any user's token) deleted on the owner's yes. Rejected: moving push-health under admin, clamping `limit` | decided; built (`7dd87b4`, `801841c`), not deployed; item 5 waits for the owner |
| [0074](0074-the-phone-name-lookup-is-closed-to-everyone-but-admins.md) | `/v1/lookup/phone` (any number → the name users saved for it: 77,625 names, none of them a user's) leaves the public list and answers ADMIN only; the contact upload itself is unchanged and stays a separate open decision. Rejected: leaving it open, a narrowed version first (the door stays open until an app release), only taking it off the list (guest tokens) | decided by the owner; being built on `fix/phone-lookup-closed` |
| [0075](0075-admin-as-user-is-deleted-support-gets-a-read-only-view.md) | `/v2/auth/admin-login-as-user` (admin password alone → any user's 24 h token, writes allowed, unused) is deleted; support gets a read-only "support view" of the user in the admin panel (their data, invoices rendered as the app does, devices, sync failures, premium, a record of who looked). Rejected: keeping it; a safer "as user" (the admin's phone still becomes a second device on the account) | decided by the owner; deletion rides with 0074; the view is to be planned |
| [0076](0076-server-logs-carry-ids-and-codes-never-a-users-values.md) | Server logs name a record by id, group, operation, version and error code — never the values a user typed (the sync push logged every property: 4,579 lines a day, kept for ever). Rejected: keeping values for debugging, masking them | decided by the owner; being built on `fix/logs-carry-no-values` |
| [0077](0077-logs-are-kept-by-user-type.md) | Logs are kept by the user's type — guests 15 days, registered 30, premium 90, no user 30 — through a 3-value `user_type` label and Loki's compactor retention (there was none; 3.3 GB since July). Rejected: a per-user label (the 643k-streams mistake), keeping everything, one age for all | decided by the owner; being built |
| [0078](0078-what-the-web-writes-reaches-the-phones.md) | Every REST (web) write that changes a synced row sets `last_synced_at` and moves `version` like a sync write, so the next delta pull brings it to every phone; the web's invoice delete takes its lines. Payments untouched (their review). Rejected: leaving web writes to the full pull, a push per web write | decided by the owner; being built on `fix/web-writes-reach-phones` |
| [0079](0079-a-deleted-record-is-kept-90-days-premium-a-year-then-purged.md) | A soft-deleted synced record is purged for good 90 days after its delete (premium: one year), only once 0059 is amended so a purged record is neither a sync failure nor re-created, with a children-first purge job and a Health Centre line. Payments wait for their form's review. Rejected: log-style windows (15/30/90), keeping deleted rows for ever | decided by the owner 2026-09-13; not built |
| [0080](0080-a-record-the-database-refuses-is-refused-alone.md) | A record the database refuses (a product name over 255 characters) is refused alone and the rest of its push is written: a push that cannot commit runs again record by record, the refused record is answered `FAILED UNEXPECTED_ERROR` with `data.field`, at most 10, then the old failure. Found: 3 guest phones stuck since 07-01 / 09-11. Rejected: a savepoint or a transaction per record, record by record always, truncating, a new error type | LIVE 2026-09-13 20:26 UTC (batch9 `1d432e6a`). For long names the owner chose both: names up to 1,000 characters are LIVE since 23:02 UTC (batch10, products and clients), and the app caps at 255 (1.4.6) |
| [0081](0081-every-money-screen-totals-in-the-chips-currency.md) | The dashboard, the client ledger and the Analytics tab total in ONE currency — the chip's (the business's own until the user picks another, remembered per business); other currencies convert at the day's rate and a converted figure carries ≈. Rejected: each screen picking the currency holding the most money (the ledger showed $ under a PKR chip; Analytics added $ as Rs) | decided by the owner 2026-09-14; into 1.4.6 |
| [0082](0082-a-clients-ledger-line-keeps-the-clients-own-currency.md) | A client's line in the ledger shows the client's own currency, as invoiced (a PKR client Rs62,500 beside a USD client $640,000); only the overview total converts, with ≈; a client whose invoices mix currencies (legacy) converts with ≈. Rejected: every line in the chip's currency (Apptrick showed $225.53 for Rs62,500, moving with the rate) | decided by the owner 2026-09-14; into 1.4.6 with 0081 |
| [0083](0083-ios-sends-its-analytics-through-the-one-shared-drain.md) | iOS empties its analytics queue through the same `AnalyticsFlusher` Android runs (moved to commonMain). iOS decides only when it runs (launch, 2 s after events, 15 min on screen, foreground, background task, background sync), and every iOS event carries a session. Found: no iPhone had ever sent an event (0 rows, ever). Rejected: an iOS-only uploader, sending only from Apple's background scheduler, rewriting queued events, guessing the platform from build numbers | built 2026-09-14 (`63f7174b`, `24134092`), merged into `VC_102_VN_146`; reaches iPhones with build 17 |
| [0084](0084-revenue-counts-real-invoices-only-overdue-from-the-due-date.md) | Every money screen uses the invoice list's rule: drafts and cancelled invoices never count toward revenue, outstanding or overdue, and overdue comes from the due date (a draft is never overdue). Covers the app's Analytics tab, the dashboard's breakdown sheet and the web dashboard. Found: Analytics Revenue differed from the dashboard's Total Revenue, and Overdue read a status nothing sets. Rejected: a rule per screen; the app only | decided by the owner 2026-09-14; web LIVE (`acc666c`); the Invoices count counts real invoices only; app → 1.4.6 |
| [0085](0085-one-lifecycle-for-both-platforms-an-update-is-not-a-first-open.md) | Android's lifecycle observer moves to shared code, and iOS runs it from UIApplication notifications, so iPhones send `app_cold_start`, foreground/background and heartbeat as Android does. An update is never a first open: with no stored count but an analytics database already on disk, the count starts at 2. Rejected: an iOS copy, counting from 1, the device id or the main database as evidence, iOS network stamps now | built 2026-09-14 (`95558880`, `0234aa09`), merged into `VC_102_VN_146`; iPhones get it in the build after 17 |
| [0086](0086-the-server-remembers-each-phones-last-pull.md) | The server keeps each phone's last pull and starts the next delta pull from there, whatever bookmark the phone sends. So builds already on phones stop skipping changes and deletes made on the web or another phone. A small migration ships first and alone. Rejected: only the app half (phones that never update keep skipping); always sending one day back | decided by the owner 2026-09-14; built after the receipt number |
| [0087](0087-ci-keeps-running-on-the-production-box.md) | CI's test, docker and deploy jobs keep running on the production VPS. Found: on 2026-09-13 a test job raised the load from 0.59 to 28.4 and 25 requests failed; 1,870 CI minutes in 30 days against GitLab's free 400. Rejected: a separate small CI server (~$5–10 a month), GitLab Premium ($29 per user per month) | decided by the owner 2026-09-14; revisit at the VPS move |
| [0088](0088-every-analytics-event-carries-its-platform.md) | `analytics_events` gains a `platform` column (Android/iOS/Web), filled by the server on arrival. Old rows are filled only with what can be proven (before the first iOS row: Android unless a web row; after it: the session's platform), and NULL where unknown. The migration goes first and alone, then the code. Rejected: waiting for the App Store launch (the recommendation), guessing from build numbers, reading the platform through sessions for ever | decided by the owner 2026-09-14; LIVE: the column 00:21 UTC (batch13), the code 00:47 UTC (batch14), backfilled 01:44–01:57 UTC (416 stay NULL) |
| [0089](0089-uxcam-leaves-the-app.md) | UXCam (session replay, gated by Remote Config `UX_Cam`, not known to be on, recordings not watched) leaves the app, together with the two unused image-crop libraries that bring test libraries and three exported test screens into release. Rejected: keeping it and checking first | decided by the owner 2026-09-14; built (`d4fb2ac7`) for 1.4.6; the `UX_Cam` key must stay empty for older builds |
| [0090](0090-the-qr-scanner-stays.md) | Device Link's QR scanner keeps Google ML Kit (323 KB, 12 opens in 30 days). Rejected: Google Code Scanner, which saves 0.32 MB but stops working on phones without Play Services and needs new testing | decided by the owner 2026-09-14 |
| [0091](0091-support-sees-a-users-app-read-only.md) | Support sees a user's app read-only: a Support tab on the user page, backed by seven admin-only reads. Emails and phones are masked on every list; names and addresses are not. A reveal needs no reason. Every look is recorded as a log line, a counter and a `support_view_log` row. Found: `/v1/ip/**` had returned full user records with no login. Rejected: continuing the old single-user pages; a search in the query string | LIVE: the table 23:54 UTC (batch12), the code 00:47 UTC (batch14 `ad625178`), the panel 00:53 UTC (`c2e414b`) |
| [0092](0092-no-password-reset-after-the-ip-route-exposure.md) | After the /v1/ip exposure (BCrypt hashes of 516 real accounts reachable with no login, 2026-04-22 → 09-13; no evidence of access in the logs that exist), no password reset and no new step for users. Rejected: an email code at the next password login (the recommendation), logging everyone out | decided by the owner 2026-09-14 ("Kuch nahi"); users not told, the record kept ("Nahi, sirf record"); tell them at once on any evidence of access |
| [0093](0093-invoices-proven-a-day-early-are-repaired-on-the-server.md) | Group A: the 1,745 invoices proven a day early (the phones' share copies show 61 of 63 exactly a day ahead; builds up to 1.4.0) move one day forward on the server, before 1.4.6, so the 1.4.6 full pull carries the right day to every phone. A dry run first, then the owner's go; an undo register (a migration, alone); a 120-day echo guard. Rejected: a repair driven by the phones (after an update they hold the wrong day), one rule for every invoice | decided by the owner 2026-09-14; **repaired 2026-09-14 09:20 UTC: 1,745 moved, 258 owners, 0 failures, run `84e1a19e`** (undo register batch15, code batch16) |
| [0094](0094-the-phone-number-name-lookup-is-removed.md) | `/v1/lookup/phone` returned a name for any phone number, taken from 509 users' uploaded address books (81,923 names). It answered with no login until 2026-09-13. It is removed from the server, and the app's Contacts "Verify" button is removed in 1.4.6. The stored names and the upload wait for the contacts-upload decision. Rejected: keeping it admin-only; narrowing it to "is this number on Invotick" (the owner's receiver requirement is built on a verified user's own identity instead) | decided by the owner 2026-09-14 ("Poora hata do"); being built |
| [0095](0095-the-app-stops-sending-the-address-book.md) | 1.4.6 stops sending the phone's whole address book, which went out when a user imported one client (530 accounts, 393,974 contacts, about 740 MB; 20–50 a day since 2026-09-03). "Import from contacts" keeps working on the phone. The server's `/v1/contacts/ingest` stays open for now and closes later. Rejected: closing the server route now too (the recommendation), keeping the upload behind a consent screen, leaving it as it is | decided by the owner 2026-09-14 ("pehly app… baad main server"); being built for 1.4.6 |
| [0096](0096-the-stored-contacts-are-kept-for-a-marketing-plan.md) | The contacts already on the server (393,974, about 740 MB) are kept, for a marketing plan the owner will make. If it works, the upload returns in a form that complies with policy. The doc records what the plan must start from: the people never agreed; Play limits use to what the user was told; PECA 2016 and foreign consent rules apply; a lawyer's review is needed before any message. Rejected: delete now (the recommendation), delete when the route closes | decided by the owner 2026-09-14 ("is ko rakho") |
| [0097](0097-the-receiver-tag-is-built-after-146-in-steps.md) | The owner's receiver requirement is built after 1.4.6, in steps: the wording and settings, then verified email, the yes/no tag in the sender's client list, then a notification and inbox that open the existing approve/reject view, then iPhone push once APNs exists. Today about 60 of 6,306 clients could match. Rejected: building it into 1.4.6, not building it for now | decided by the owner 2026-09-14. The same day: wording "On Invotick"; identity comes automatically from the user's own actions (nobody is asked to verify); the receiver gets a "Show that I'm on Invotick" setting, on by default; the goal behind it is automatic payment-due messages (in the app and by WhatsApp); the receiver hears only when the sender shares or sends a reminder ("Sirf jab sender bheje") |
| [0098](0098-a-session-the-app-ended-reopens-with-the-email-filled.md) | A1: after the 2026-09-03 key change (the old key had leaked), about 17 registered accounts still meet a sign-in screen. No server change; the old key never returns. In 1.4.6, when the app ended the session itself, the existing sign-in screen opens in a "sign in again" mode: a line saying the invoices are safe, the email filled in (kept through an involuntary sign-out only), the password one tap away from Google's or Apple's password manager, and no "Login as Guest". Rejected: accepting the old key for a while, a silent re-issue, keeping the password on the phone, wording only | decided by the owner 2026-09-14; being built for 1.4.6 |
| [0099](0099-a-phone-cannot-remove-itself.md) | A guest phone removed itself from its own Linked devices screen (2026-09-13), because the app never told the server which phone was asking. Its syncs have been refused ever since, and its work is stranded. The server now refuses a revoke that doesn't name the calling phone (builds up to 1.4.6 can't remove devices until they update); a removed phone gets 403 and no new pass; every refusal names its phone and build. That guest's one row is re-admitted after the deploy. In 1.4.6 the app names itself, and a removed phone stops, keeps its data and says so. Rejected: the guard only, nothing | decided by the owner 2026-09-14 ("Dono karo"); batch17, then the re-admit, then 1.4.6. Same day: a correct password sign-in re-admits a removed phone ("Haan, password se wapas") |
| [0100](0100-phones-delete-their-cached-address-book-once.md) | Up to 1.4.5, "Import from contacts" copied the whole address book into the app's own database before uploading it. The picker never used that copy, since it reads the phone fresh each time. The copy stays on phones, survives sign-out, goes into phone backups, and outlives the user's permission. 1.4.6 deletes it once, at the first open after the update. Contacts, saved clients and the picker are untouched. Rejected: leaving it | decided by the owner 2026-09-14 ("Haan, aik dafa mita do"); being built for 1.4.6 |
| [0101](0101-a-renewal-key-renews-a-pass-without-signing-in.md) | Each signed-in phone holds a random renewal key, which the server keeps only as a hash, so a signing-key change or a 90-day absence no longer forces a sign-in. The phone exchanges it once for a new pass and key; a replayed key revokes that phone's keys. Removal, deliberate sign-out and a password change cancel it. The app tries it before the sign-in-again screen. Kill switch on both sides. Rejected: after 1.4.6 (the recommendation), not at all. Design, the same day ("Saboot wala tareeqa"): proof of receipt instead of a timer. The old key stays usable until the new pass is first used, for 7 days at most. Theft cancels keys and passes. 180 days unused ends a key | decided by the owner 2026-09-14 ("1.4.6 mein hi"); server built, design change being made; its table on the owner's word |
| [0102](0102-import-from-contacts-uses-the-phones-own-picker.md) | "Import from contacts" opens the phone's own contact picker (Android's picker, `ACTION_PICK` on older versions, and iOS's `CNContactPickerViewController`). The app receives only the picked contact and stops asking to read the whole address book, which also removes a popup on the way to a first invoice. Google Play makes `READ_CONTACTS` restricted (mandatory on 2027-01-27 at target 37). 0096's whole-book upload cannot use this path. Rejected: after 1.4.6 (the recommendation), keeping the permission | decided by the owner 2026-09-14 ("1.4.6 mein hi"); **built on `VC_102_VN_146` (`8f259399`)**. Same day: older Android gets the name and one number, and the contacts type-ahead goes ("Theek hai, aise hi") |
| [0103](0103-meta-audience-network-joins-admob-mediation.md) | The Meta Audience Network adapter joins the app's AdMob mediation (Android, 1.4.6), so Meta can bid for the app's ad slots. A separate agent tests it on a device. The console side (Meta placements, AdMob mediation groups, Data safety, app-ads.txt if needed) is the owner's, from written steps. The gain (`ad_source`) and the cost (size, start-up, crashes) are both measured | the owner's instruction 2026-09-14; being built |
| [0104](0104-a-payment-date-travels-as-a-calendar-day.md) | A payment's date leaves the app as `YYYY-MM-DD`, like invoices, instead of an instant whose UTC day is the day before for payments entered after midnight in the east. Since 08-28, 107 of 116 payments carried their own day and 9 the day before (not proven to be this). The server reads both shapes and counts them, and ships first. A narrow carve-out from the postponed Payments review: no screen, totals, logic or stored payment changes. Rejected: waiting for the review | decided by the owner 2026-09-14 ("Sirf tareekh abhi theek karo"); being built |
| [0105](0105-remote-screens-are-built-into-146.md) | The owner's remote-screens idea is built into 1.4.6. Screens are designed in the admin panel and shown in the app: phase 1 is the pipe plus a first announcement (server table, panel Screens page with preview, and app blocks), about 3 weeks. The stores allow it if the phone downloads data only and no remote screen asks for a password or payment. Rejected: after 1.4.6 (the recommendation), not now | decided by the owner 2026-09-14 ("Haan, 1.4.6 mein hi"); its table on the owner's word; seven how-decisions to ask |
| [0107](0107-the-premium-screen-promises-only-what-the-app-does.md) | The premium screen lists 12 benefits, and only "No Ads" is real and premium-only. "Auto follow-ups", "Shareable payment links" and "Priority Cloud Sync" exist in no build, and have been listed in every release since 1.3.9. They come off the screen in 1.4.6, with no "coming soon". The chips become Play's "10K+ Downloads", with no rating. Only "No Ads" is shown as Premium, and the rest is listed as free for everyone. The "7-day refund" pill goes. 305 phones saw the screen in 20 days. Play requires subscription benefits to be truthful | decided in four parts by the owner 2026-09-14; built into 1.4.6 |
| [0106](0106-every-date-the-apps-send-is-watched-until-it-arrives-as-a-day.md) | A Health Centre card, "Invoice dates arrive as calendar days", turns red when a build that should send an invoice's or a payment's date as `YYYY-MM-DD` sends anything else: for an invoice every numbered build, every iPhone and the web; for a payment Android 102 and iOS 19 on. It reads Prometheus and adds each new series' first count, which `increase()` misses. An app test pins all six dates on the wire in Karachi, New York and Tonga. Rejected: a tally table, `increase()` alone, judging by `versionCode ≥ 91` (it reads an iPhone as an old build) | decided by the sync agent on the owner's ask, 2026-09-14; built, not deployed |
